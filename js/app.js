window.__resources = {"porteChapelierImg": "assets/images/porteChapelierImg.svg", "porteLapinImg": "assets/images/porteLapinImg.svg", "porteFlamantImg": "assets/images/porteFlamantImg.svg", "porteSoldatImg": "assets/images/porteSoldatImg.svg", "porteChatImg": "assets/images/porteChatImg.svg", "handImg": "assets/images/handImg.webp", "kompaPeekImg": "assets/images/kompaPeekImg.webp", "e7vaudio": "assets/audio/e7vaudio.mp3", "e6audio": "assets/audio/e6audio.mp3", "e5audio": "assets/audio/e5audio.mp3", "e1audio": "assets/audio/e1audio.mp3", "e6vaudio": "assets/audio/e6vaudio.mp3", "e8audio": "assets/audio/e8audio.mp3", "lantVert0Off": "assets/images/lantVert0Off.webp", "e3audio": "assets/audio/e3audio.mp3", "lantOrangeOff": "assets/images/lantOrangeOff.webp", "lantRoseOff": "assets/images/lantRoseOff.webp", "lantVertOff": "assets/images/lantVertOff.webp", "lantBleuOff": "assets/images/lantBleuOff.webp", "lantOrangeOn": "assets/images/lantOrangeOn.webp", "porteSourisImg": "assets/images/porteSourisImg.webp", "e4audio": "assets/audio/e4audio.mp3", "lantVertOn": "assets/images/lantVertOn.webp", "e4vaudio": "assets/audio/e4vaudio.mp3", "porteRougeImg": "assets/images/porteRougeImg.webp", "reveilImg": "assets/images/reveilImg.webp", "e3vaudio": "assets/audio/e3vaudio.mp3", "lantBleuOn": "assets/images/lantBleuOn.webp", "papillonLow": "assets/images/papillonLow.webp", "e7audio": "assets/audio/e7audio.mp3", "e2audio": "assets/audio/e2audio.mp3", "lantVert0On": "assets/images/lantVert0On.webp", "lantRoseOn": "assets/images/lantRoseOn.webp",
"doigtGauche": "assets/images/doigt_gauche.png", "doigtDroit": "assets/images/index-papercraft-dos-main-droite-sansfond-v1.png", "doigtDroitV3": "assets/images/index-papercraft-dos-v3.png",
"labyrintheB": "assets/images/labyrinthe-m1-v3.png", "papillonB": "assets/images/papillon-kompa-inverse.png",
"fioleB": "assets/images/fiole-drinkme-origami-v1.png",
"presentoirVide": "assets/images/presentoir-vide-fil-origami-v1.png", "presentoirComplet": "assets/images/presentoir-complet-fil-origami-v1.png", "presentoirFinal": "assets/images/presentoir-final-origami-v1.png",
"itemCupcake": "assets/images/item-cupcake-origami-v3.png", "itemTheiere": "assets/images/item-theiere-origami-v3.png", "itemMontre": "assets/images/item-montre-origami-v3.png", "itemCle": "assets/images/item-cle-origami-v3.png", "itemGateau": "assets/images/item-gateau-origami-v3.png", "itemTasse": "assets/images/item-tasse-origami-v3.png",
"kompaPeekGauche": "assets/images/kompa_peek_gauche-9a5c8893.png",
"lapinBlancImg": "assets/images/lapinBlancImg.svg", "chatCheshireImg": "assets/images/chatCheshireImg.svg",
"coffreBonbonsImg": "assets/images/coffreBonbonsImg.svg",
"iconSuivreImg": "assets/images/iconSuivreImg.svg", "iconSiesteImg": "assets/images/iconSiesteImg.svg", "iconCarotteImg": "assets/images/iconCarotteImg.svg",
"carteGardeImg": "assets/images/carteGardeImg.svg", "carteReposImg": "assets/images/carteReposImg.svg",
"pieceOrImg": "assets/images/pieceOrImg.svg", "buissonImg": "assets/images/buissonImg.svg",
"coffreTresorFinalImg": "assets/images/coffreTresorFinalImg.svg", "fumeeImg": "assets/images/fumeeImg.svg",
"deuxDoigtsScrollImg": "assets/images/deuxDoigtsScrollImg.svg",
"iconFermerImg": "assets/images/iconFermerImg.svg", "iconSupprimerImg": "assets/images/iconSupprimerImg.svg",
"scratchIconImg": "assets/images/scratchIconImg.png", "scratchFenetreImg": "assets/images/scratchFenetreImg.png", "bureauScratchImg": "assets/images/bureauScratchImg.png",
"scratchDemoVideo": "assets/images/scratchDemoVideo.mp4"
};

const MOUSE_SRC="data:image/svg+xml;base64,",
/* Mascotte Professeur Pile Poil — PLACEHOLDER temporaire.
   Pour remettre ta vraie mascotte : remplace la valeur ci-dessous
   par "data:image/png;base64,XXXX" (ta vraie image). */
KOMPA_SRC="assets/images/kompaSrc.webp";

/* Nom affiché/lu de la mascotte. Les identifiants techniques (kompaEl, kompaPeekImg, KOMPA_SRC,
   classes CSS .kompa-*, noms de fichiers) restent inchangés : seul le texte visible porte ce nom. */
const PROF_NOM="Professeur Pile Poil";


const PAP_LOWPOLY=window.__resources.papillonLow;
/* Curseur (flèche) qui suit le doigt sur le trackpad — remplace l'ancienne souris physique */
const CURSOR_SRC="data:image/svg+xml,"+encodeURIComponent(`<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><path d='M22 14 L22 82 L41 64 L53 90 L66 84 L54 59 L78 59 Z' fill='#ffffff' stroke='#111111' stroke-width='6' stroke-linejoin='round' stroke-linecap='round'/></svg>`);
const FINGER_SRC="data:image/svg+xml,"+encodeURIComponent(`<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 80 120'><g fill='#cfcfcf' stroke='#111111' stroke-width='6' stroke-linejoin='round' stroke-linecap='round'><path d='M30 118 L30 60 C30 40 33 20 40 20 C47 20 50 40 50 60 L50 118 Z'/><ellipse cx='40' cy='30' rx='11' ry='9'/></g></svg>`);

/* Trackpad dessiné (deux boutons en bas). colored:true → bouton gauche bleu, droit orange. */
let _tpN=0;
function trackpadHTML(opts){
  opts=opts||{}; const id='tpc'+(++_tpN);
  const lf=(opts.colored||opts.leftOnly)?'var(--bleu)':'#ffffff', rf=(opts.colored||opts.rightOnly)?'var(--orange)':'#ffffff';
  return `<svg class="trackpad-svg" viewBox="0 0 600 460" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
    <defs><clipPath id="${id}"><rect x="40" y="30" width="520" height="400" rx="46"/></clipPath></defs>
    <g clip-path="url(#${id})">
      <rect x="40" y="30" width="520" height="400" fill="#ffffff"/>
      <rect class="tp-left" x="40" y="322" width="260" height="112" fill="${lf}"/>
      <rect class="tp-right" x="300" y="322" width="260" height="112" fill="${rf}"/>
    </g>
    <rect x="40" y="30" width="520" height="400" rx="46" fill="none" stroke="#111111" stroke-width="6"/>
    <line x1="40" y1="322" x2="560" y2="322" stroke="#111111" stroke-width="5"/>
    <line x1="300" y1="322" x2="300" y2="430" stroke="#111111" stroke-width="5"/>
  </svg>`;
}

/* =======================================================================
   ✏️  CONFIG — TOUT LE CONTENU MODIFIABLE EST ICI
   ======================================================================= */
const CONFIG = {
  ui: {
    progressLabel:"NIVEAU A",
    btnRetour:"◀ Retour",
    btnSuivant:"Suivant ▶",
    btnOuvrir:"Ouvrir les portes ▶",
    btnCommencer:"C'est parti ▶",
    legendeEcoute:"🔊 Écoute bien le "+PROF_NOM+" !",
    titreNotion:"Le Permis de la Souris",
    titrePhaseTest:"Suis le papillon",
    titrePresentationClic:"Le clic gauche",
    legendeBleu:"Bouton bleu = clic gauche",
    legendeOrange:"Bouton orange = clic droit (plus tard)",
    titreVictoire:"Bravo !",
    badgeTitre:"Badge débloqué :\ndéplacement & clic gauche"
  },

  /* ===========================================================
     🎙️  ORAUX — À INTÉGRER TOI-MÊME
     Tous les champs "oral..." / "consigne" / "reussi" / "sinon"
     sont VIDES. Colle le texte de chaque réplique entre les "".
     (Les textes affichés à l'écran "ecran..." sont déjà remplis.)
     =========================================================== */

  // ÉCRAN 1 — Présentation de la notion (déplacement)
  presentationNotion: {
    oral: "",                                          // ← ORAL : présentation de la notion
    ecran: "La souris et le trackpad :<br>deux outils pour bouger le curseur."
  },
  // Présentation du clic gauche
  presentationClic: {
    oral: "",                                          // ← ORAL : présentation du clic gauche
    ecran: "Le clic gauche, c'est le bouton bleu.\nUn seul appui pour choisir et ouvrir."
  },

  badge: {
    oralTeaser:"",                                     // ← ORAL : teaser du badge / victoire
    ecran:"Niveau A réussi : déplacement et clic gauche maîtrisés !\n\nProchaine étape : apprends à garder le bouton appuyé pour attraper et déplacer."
  }
};
/* ===================== FIN DE LA ZONE MODIFIABLE ===================== */

/* Son */
let sonOn=true, frVoice=null, AC=null;
const sonBtn=document.getElementById('sonBtn');
sonBtn.onclick=()=>{ sonOn=!sonOn; sonBtn.textContent=sonOn?'Son : activé':'Son : coupé'; sonBtn.classList.toggle('on',!sonOn); if(!sonOn){ if(window.speechSynthesis) speechSynthesis.cancel(); stopOral(); } if(sonOn){ try{ AC=AC||new (window.AudioContext||window.webkitAudioContext)(); if(AC.state==='suspended') AC.resume(); }catch(e){} try{ speechSynthesis.resume(); }catch(e){} } if(!sonOn){ musiqueStop(); } else { musiqueStart(); musiqueDuck(!(pqEl && pqEl.classList.contains('on'))); } };
function pickVoice(){
  const v=speechSynthesis.getVoices();
  const fr=v.filter(x=>x.lang&&x.lang.toLowerCase().startsWith('fr'));
  // Préférer une voix masculine française (noms varient selon l'appareil)
  const males=['thomas','paul','nicolas','daniel','henri','jean','antoine','guy','rémy','remy','google français'];
  frVoice = fr.find(x=>males.some(n=>x.name.toLowerCase().includes(n)))
         || fr.find(x=>/(male|homme)/i.test(x.name))
         || fr[0] || null;
}
if('speechSynthesis' in window){ pickVoice(); speechSynthesis.onvoiceschanged=pickVoice; }
function estimDuree(t){ return t? Math.max(2500, Math.min(70000, t.length*62)) : 3500; }
function parle(t, onend){
  const fin=()=>{ if(onend){ const f=onend; onend=null; f(); } };
  if(!sonOn||!('speechSynthesis'in window)||!t){ if(onend) setTimeout(fin, sonOn?estimDuree(t):4000); return; }
  try{ speechSynthesis.cancel(); }catch(e){}
  const u=new SpeechSynthesisUtterance(t); u.lang='fr-FR'; if(frVoice)u.voice=frVoice; u.rate=0.9; u.pitch=0.8;
  if(onend){ u.onend=fin; setTimeout(fin, estimDuree(t)+5000); }
  try{ speechSynthesis.resume(); }catch(e){}
  setTimeout(()=>{ try{ speechSynthesis.speak(u); }catch(e){} }, 70);
}

/* Lecture des répliques enregistrées (voix Paul K) — désactivée (navigation manuelle) */
const oralAudio=new Audio(); oralAudio.preload="auto";
let oralStopTimer=null, oralToken=0;
function stopOral(){ oralToken++; try{ oralAudio.pause(); }catch(e){} if(oralStopTimer){ clearTimeout(oralStopTimer); oralStopTimer=null; } oralAudio.onended=null; oralAudio.onloadedmetadata=null; }
function playOral(key, onend){
  /* voix off désactivée (navigation manuelle) */
  stopOral();
}
function bip(f,d=0.1,ty='square'){ if(!sonOn) return; try{ AC=AC||new (window.AudioContext||window.webkitAudioContext)(); const o=AC.createOscillator(),g=AC.createGain(); o.type=ty;o.frequency.value=f;o.connect(g);g.connect(AC.destination); g.gain.setValueAtTime(0.18,AC.currentTime); g.gain.exponentialRampToValueAtTime(0.001,AC.currentTime+d); o.start();o.stop(AC.currentTime+d);}catch(e){} }
const bipClic=()=>bip(660,0.09), bipOk=()=>{bip(523,0.1);setTimeout(()=>bip(784,0.16),90);}, bipNon=()=>{ bip(180,0.16,'sawtooth'); secoueEcran(); };
/* Vibration de l'écran : nudge quand le curseur ne suit pas / sur une erreur */
function secoueEcran(){ const m=document.getElementById('main'); if(!m) return; m.classList.remove('secoue'); void m.offsetWidth; m.classList.add('secoue'); }

/* === Voix du Professeur Pile Poil — clips E1..E8 par écran, + E_V/E_D pour les missions === */
const ECRAN_AUDIO=[window.__resources.e1audio,window.__resources.e2audio,window.__resources.e3audio,window.__resources.e4audio,window.__resources.e5audio,window.__resources.e6audio,window.__resources.e7audio,window.__resources.e8audio];
const MISSION_V={2:window.__resources.e3vaudio,3:window.__resources.e4vaudio,5:window.__resources.e6vaudio,6:window.__resources.e7vaudio};
const ecranAudio=new Audio(); ecranAudio.preload='auto';
const vdAudio=new Audio(); vdAudio.preload='auto';
let ecranToken=0;
function stopEcranAudio(){ ecranToken++; try{ ecranAudio.pause(); }catch(e){} ecranAudio.onended=null; ecranAudio.ontimeupdate=null; }
function stopToutAudio(){ stopEcranAudio(); try{ vdAudio.pause(); }catch(e){} vdAudio.onended=null; }
function jouerEcran(i, onTime, onEnd){
  stopEcranAudio();
  const src=ECRAN_AUDIO[i]; if(!src||!sonOn){ if(onEnd) setTimeout(onEnd, 800); return; }
  const my=++ecranToken;
  ecranAudio.src=src;
  if(onTime) ecranAudio.ontimeupdate=()=>{ if(my===ecranToken) onTime(ecranAudio.currentTime); };
  ecranAudio.onended=()=>{ if(my===ecranToken && onEnd) onEnd(); };
  const pr=ecranAudio.play(); if(pr&&pr.catch) pr.catch(()=>{ if(my===ecranToken && onEnd) setTimeout(onEnd,1200); });
}
function jouerVictoire(i, onEnd){
  stopEcranAudio();
  const clip=MISSION_V[i];
  try{ vdAudio.pause(); }catch(e){}
  vdAudio.onended=null;
  if(!clip||!sonOn){ if(onEnd) setTimeout(onEnd, 600); return; }
  vdAudio.currentTime=0; vdAudio.src=clip;
  vdAudio.onended=()=>{ if(onEnd) onEnd(); };
  const pr=vdAudio.play(); if(pr&&pr.catch) pr.catch(()=>{ if(onEnd) setTimeout(onEnd,1200); });
}
function animerPastilles(){
  const nodes=Array.from(document.querySelectorAll('#steps .snode'));
  nodes.forEach((el,idx)=>{ setTimeout(()=>{ el.classList.add('zoom-pastille'); setTimeout(()=>el.classList.remove('zoom-pastille'), 420); }, idx*550); });
}

/* État + navigation */
const main=document.getElementById('main'), souris=document.getElementById('souris');
souris.src=CURSOR_SRC;
let current={kind:null,index:0}, inactivite=null, suiviSouris=false;
let els={}, sinonCourant=null;
const U=CONFIG.ui;
const cssVar=v=>getComputedStyle(document.documentElement).getPropertyValue(v).trim();

/* Fiole de progression */
const stepNodes=document.querySelectorAll('#steps .snode');
const trackFill=document.getElementById('trackfill');
let progN=0;
function setProgress(n, active){
  n=Math.max(0,Math.min(5,n));
  stepNodes.forEach(el=>{
    const i=+el.getAttribute('data-i');
    el.classList.toggle('done', i<n);
    el.classList.toggle('cur', i===n && !!active);
  });
  // le trait se remplit entre les chiffres : 4 segments entre les 5 numéros
  const seg = n<=1 ? 0 : Math.min(1,(n-1)/3);
  if(trackFill) trackFill.style.width = "calc((100% - 36px) * "+seg+")";
  progN=n;
}
/* Raccourcis : cliquer une pastille pour sauter à sa mission (une fois le niveau validé) */
const PASTILLE_CIBLE = {1:2, 2:3, 3:5, 4:6};
function pastillesActives(){ try{ return pqGetFaits().includes('A'); }catch(e){ return false; } }
function majPastilles(){ const s=document.getElementById('steps'); if(s) s.classList.toggle('cliquable', pastillesActives()); }
stepNodes.forEach(el=>{ el.addEventListener('click', ()=>{ if(!pastillesActives()) return; const i=+el.getAttribute('data-i'); const cible=PASTILLE_CIBLE[i]; if(cible!=null){ if(typeof bipClic==='function') bipClic(); allerA(cible); } }); });
function clearInactivite(){ if(inactivite){clearTimeout(inactivite);inactivite=null;} }
function armeInactivite(key){ clearInactivite(); if(!key) return; inactivite=setTimeout(()=>playOral(key), 5000); }
let mx=window.innerWidth/2, my=window.innerHeight/2, lastMoveT=0;
document.addEventListener('mousemove', e=>{ mx=e.clientX; my=e.clientY; lastMoveT=performance.now(); if(suiviSouris){ souris.style.left=e.clientX+'px'; souris.style.top=e.clientY+'px'; } if(sinonCourant) armeInactivite(sinonCourant); });
function activerSuivi(on){ suiviSouris=on; souris.style.display=on?'block':'none'; if(on && !souris.style.left){ souris.style.left=(window.innerWidth/2)+'px'; souris.style.top=(window.innerHeight/2)+'px'; } }
/* === Professeur Pile Poil — vidéos animées (expressions labiales synchronisées à la voix off) ===
   Dépose les fichiers .mp4/.webm à côté d'index.html et renseigne les chemins ci-dessous.
   Laisser "" garde l'image fixe en repli (le poster = image fixe pendant le chargement). */
const KOMPA_POSTER=KOMPA_SRC;
const KOMPA_VIDEOS={
  parle:'',     // Kompa qui parle (écrans de présentation / explication)
  pres:'',      // Kompa sur le côté (écran 1, présentation de la notion)
  droite:'',    // Kompa content (écran de victoire)
  peek:'',      // Kompa qui passe la tête (phase test)
  parcours:''   // Kompa sur la page d'accueil / parcours des niveaux
};
function kompaEl(cls, id, key, poster){
  const src=KOMPA_VIDEOS[key||'parle']; const p=poster||KOMPA_POSTER;
  const idAttr=id?` id="${id}"`:'';
  if(src){ return `<video class="${cls}"${idAttr} src="${src}" poster="${p}" muted loop playsinline autoplay preload="auto" aria-label="${PROF_NOM}"></video>`; }
  return `<img class="${cls}"${idAttr} src="${p}" alt="${PROF_NOM}">`;
}
function kompaCentre(extra){ return kompaEl('kompa-centre '+(extra||''), 'kompaC', 'parle'); }
function montreCarte(){}
function cacheCarte(){}
function kompaDroite(){ return kompaEl('kompa-droite', null, 'droite'); }
function setHeaderTools(visible){ // masque Son + Retour (ex. sur l'écran de victoire)
  if(retourBtn) retourBtn.style.display = visible ? '' : 'none';
  sonBtn.style.display = visible ? '' : 'none';
}
let tempoTimer=null, libreIv=null, papRaf=null;
function clearTempo(){ if(tempoTimer){ clearTimeout(tempoTimer); tempoTimer=null; } }
/* ===== Effet de clic commun a tout le jeu =====
   Un clic (simule dans les demos) doit se VOIR : la zone pressee s'eclaircit brievement,
   puis revient a sa couleur. Utilise par les niveaux A, B et C pour une lecture identique. */
const CLIC_FLASH_MS=220;
function flashClic(zone, couleurNormale){
  if(!zone) return;
  if(zone.__flashTO) clearTimeout(zone.__flashTO);
  zone.setAttribute('fill','#ffffff');
  zone.__flashTO=setTimeout(()=>{ zone.setAttribute('fill', couleurNormale); zone.__flashTO=null; }, CLIC_FLASH_MS);
}
/* Maintien enfonce (niveau B) : la zone reste eclaircie tant que le doigt appuie. */
function pressClic(zone, couleurNormale, enfonce){
  if(!zone) return;
  if(zone.__flashTO){ clearTimeout(zone.__flashTO); zone.__flashTO=null; }
  zone.setAttribute('fill', enfonce ? '#ffffff' : couleurNormale);
}
function clearFlashIv(){ if(window.__flashIv){ clearInterval(window.__flashIv); window.__flashIv=null; } }
function tempo(ms, fn){ clearTempo(); /* passage auto désactivé : navigation par bouton */ }
function cacheKompa(){ cacheCarte(); const k=document.getElementById('kompaC'); if(k){ k.classList.remove('parle'); if(k.tagName==='VIDEO'){ try{ k.pause(); }catch(e){} } k.classList.add('partie'); setTimeout(()=>{ if(k) k.style.display='none'; }, 320); } }
function kompaParleOn(){ const k=document.getElementById('kompaC'); if(k){ k.classList.add('parle'); if(k.tagName==='VIDEO'){ try{ k.currentTime=0; const p=k.play(); if(p&&p.catch) p.catch(()=>{}); }catch(e){} } } }
const FELICITATIONS=["Bravo !","Bien joué !","Super !","Excellent !","Trop fort !","Génial !","Champion !","Tu assures !"];
function animReussite(custom){ const msg=custom||FELICITATIONS[Math.floor(Math.random()*FELICITATIONS.length)]; const o=document.createElement('div'); o.id='succes'; o.innerHTML=`<div class="carte-succes"><span class="coche">✓</span><span class="msg">${msg}</span></div>`; document.body.appendChild(o); setTimeout(()=>{ o.style.opacity='0'; setTimeout(()=>o.remove(),400); }, 1500); }

/* === Médaille (image fournie, fond transparent) === */
const MEDAILLE_SVG=`<img src="assets/images/medaille.webp" alt="Médaille Permis Souris">`;

const parcours=[ ()=>renderPresentationNotion(), ()=>renderPhaseTest(), ()=>renderMission1(), ()=>renderMission2(), ()=>renderPresentationClic(), ()=>renderMission3(), ()=>renderMission4(), ()=>renderBadge() ];
let etape=0;
function allerA(i){
  const cible=Math.max(0,Math.min(parcours.length-1,i));
  const run=()=>{
    etape=cible; clearInactivite(); clearTempo(); clearFlashIv(); sinonCourant=null; if(libreIv){clearInterval(libreIv); libreIv=null;} if(papRaf){cancelAnimationFrame(papRaf); papRaf=null;} if(window.lapRaf){cancelAnimationFrame(window.lapRaf); window.lapRaf=null;} if(window.speechSynthesis) speechSynthesis.cancel(); stopOral(); stopToutAudio(); activerSuivi(false); cacheCarte(); setHeaderTools(true); main.style.opacity='1'; parcours[etape](); majRetour();
  };
  if(main.firstElementChild){ main.style.transition='opacity .32s ease'; main.style.opacity='0'; setTimeout(run,320); } else run();
}
const retourBtn=document.getElementById('retourBtn');
if(retourBtn) retourBtn.onclick=()=>allerA(etape-1);
function majRetour(){ if(retourBtn){ retourBtn.textContent=U.btnRetour; retourBtn.disabled=(etape===0); } }
function majProg(){ const l=document.getElementById('progLabel'); if(l){ l.textContent=U.progressLabel; l.style.color='#4A4A4A'; } majPastilles(); }

/* ÉCRAN 1 — Présentation de la notion (déplacement) : Kompa au centre explique */
function renderPresentationNotion(){
  current={kind:'presentationNotion'}; setProgress(0); majProg();
  const P=CONFIG.presentationNotion;
  main.innerHTML=`<div class="screen active pres1" style="top:24px">
    <h1 id="ecranBox" style="position:relative; top:20px; align-self:flex-start; margin:6px 0 0">${P.ecran}</h1>
    <div class="pres-row">
      ${kompaEl('pres-kompa','kompaC','pres')}
      <div class="device tp"><span class="dname">Le Trackpad</span>${trackpadHTML()}</div>
      <div class="device ms"><span class="dname">La Souris</span><img class="souris-img" src="${MOUSE_SRC}" alt="souris"></div>
    </div>
  </div>`;
  els={ titre:document.getElementById('titre'), ecran:document.getElementById('ecranBox'), apres:document.getElementById('apres') };
  activerSuivi(false);   // la souris n'apparaît pas pendant la présentation de la notion
  if(els.apres) els.apres.style.opacity='1';
  kompaParleOn();
  let zoomFaitM=false, pastillesFaitM=false;
  jouerEcran(0, (t)=>{
    if(t>=18 && !zoomFaitM){
      zoomFaitM=true;
      const tp=document.querySelector('.device.tp'), svg=document.querySelector('.device.tp .trackpad-svg');
      if(svg){
        const r=svg.getBoundingClientRect(); const dx=(window.innerWidth/2)-(r.left+r.width/2), dy=(window.innerHeight/2)-(r.top+r.height/2);
        svg.style.setProperty('--zx',dx+'px'); svg.style.setProperty('--zy',dy+'px');
        svg.classList.add('zoom-tp'); if(tp) tp.classList.add('zoom-tp');
        const ms=document.querySelector('.device.ms'); if(ms) ms.classList.add('zoom-hide');
        setTimeout(()=>{ svg.classList.remove('zoom-tp'); if(tp) tp.classList.remove('zoom-tp'); if(ms) ms.classList.remove('zoom-hide'); }, 3200);
      }
    }
    const dur=ecranAudio.duration||0;
    if(!pastillesFaitM && (t>=32 || (dur && dur-t<=2.4))){ pastillesFaitM=true; animerPastilles(); }
  }, ()=>{ cacheKompa(); allerA(etape+1); });
}

/* ÉCRAN 2 — Phase de test : Kompa se décale → la souris s'active tout de suite, le cadre monte,
   l'élève suit le papillon sur la même page pendant que Kompa finit de parler. */
function laptopSVG(){
  let keys='';
  for(let r=0;r<3;r++){ const ky=388+r*12; for(let x=210;x<=592;x+=20){ keys+=`<rect x='${x}' y='${ky}' width='16' height='7' rx='1.5' fill='#fff' stroke='#111' stroke-width='2'/>`; } }
  const hand=`<image id="lapHand" href="${window.__resources.handImg}" width="150" height="187" transform="translate(325,398)"/>`;
  return `<svg class="lap-svg" viewBox="0 0 800 720" xmlns="http://www.w3.org/2000/svg">
    <g fill="#ffffff" stroke="#111111" stroke-width="5" stroke-linejoin="round">
      <rect x="110" y="14" width="580" height="360" rx="26" fill="#1B4E30"/>
      <rect x="140" y="48" width="520" height="298" rx="10" stroke-width="4" fill="#ffffff"/>
      <circle cx="400" cy="31" r="5" stroke-width="3" fill="#ffffff"/>
      <polygon points="180,380 620,380 704,496 96,496" fill="#66438D"/>
      <polygon points="96,496 704,496 690,526 110,526"/>
      <rect x="300" y="440" width="200" height="46" rx="6" stroke-width="3"/>
    </g>
    ${keys}
    <image id="lapCursor" href="${CURSOR_SRC}" width="40" height="40" transform="translate(391,150)"/>
    ${hand}
  </svg>`;
}
function renderPhaseTest(){
  current={kind:'phaseTest'}; setProgress(0); majProg();
  activerSuivi(false); cacheKompa();
  main.innerHTML=`<div class="screen active lap2">
    <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px">Passe ton Permis du Trackpad avant de devenir codeur !</div>
    <div class="lap-wrap">${laptopSVG()}</div>
    ${kompaEl('kompa-peek','kompaPeek','peek', window.__resources.kompaPeekImg)}
    <img class="pap-arrive" id="papArrive" src="${PAP_LOWPOLY}" alt="papillon">
  </div>`;
  const hand=document.getElementById('lapHand'), cursor=document.getElementById('lapCursor');
  if(window.lapRaf) cancelAnimationFrame(window.lapRaf);
  const t0=performance.now();
  const anim=(now)=>{ const t=(now-t0)/1000;
    const fx=0.5+0.42*Math.sin(t*0.9), fy=0.5+0.34*Math.sin(t*1.3+0.7);
    const tpx=340+fx*(460-340), tpy=446+fy*(478-446);
    const cx=152+fx*(648-152), cy=60+fy*(336-60);
    if(hand) hand.setAttribute('transform',`translate(${tpx-75},${tpy-18})`);
    if(cursor) cursor.setAttribute('transform',`translate(${cx-9},${cy-6})`);
    window.lapRaf=requestAnimationFrame(anim); };
  window.lapRaf=requestAnimationFrame(anim);
  jouerEcran(1, (t)=>{
    if(t>=14){ const kp=document.getElementById('kompaPeek'); if(kp) kp.classList.add('in'); }
    if(t>=22){ const pa=document.getElementById('papArrive'); if(pa && !pa.classList.contains('in')){ pa.classList.add('in'); setTimeout(()=>pa.classList.add('embete'), 1150); } }
  }, ()=>allerA(etape+1));
}
/* Présentation clic gauche */
function renderPresentationClic(){
  current={kind:'presentationClic'}; setProgress(3); majProg();
  const P=CONFIG.presentationClic;
  main.innerHTML=`<div class="screen active">
    <h1 id="titre">${U.titrePresentationClic}</h1>
    <div id="apres" class="panneau-droit" style="opacity:0">
      <div class="legende">
        <div class="souris-wrap tp">
          ${trackpadHTML({colored:true})}
          <div class="clic-effet" style="display:none">
            <span class="clic-zone"></span>
            <span class="clic-ring"></span>
            <span class="clic-ring r2"></span>
            <img class="main-tap" src="${window.__resources.handImg}" alt="doigt" style="width:clamp(80px,11vw,150px); height:auto">
          </div>
        </div>
      </div>
      <div class="ecran" id="ecranBox" style="margin-top:14px; font-size:clamp(14px,1.5vw,19px); padding:10px 14px">${P.ecran}</div>
    </div>
    ${kompaCentre('gauche')}
  </div>`;
  els={ titre:document.getElementById('titre'), ecran:document.getElementById('ecranBox'), legB:document.getElementById('legB'), legO:document.getElementById('legO'), apres:document.getElementById('apres') };
  if(els.apres) els.apres.style.opacity='1';
  // D'abord la souris seule (taille normale, sans la main ni l'effet).
  // Puis, au moment où Kompa parle du clic gauche : zoom + apparition de la main qui appuie + effet.
  setTimeout(()=>{
    // Garde-fou : si l'ecran a change entre-temps, ne rien toucher (un autre ecran affiche
    // aussi un trackpad et serait modifie a tort par ce minuteur devenu obsolete).
    if(current.kind!=='presentationClic') return;
    const ef=document.querySelector('.clic-effet');
    if(ef) ef.style.display='block';
    // Meme effet de clic que partout ailleurs : la zone bleue s'eclaire a chaque appui,
    // au rythme de l'anneau anime (1,4 s) deja utilise sur cet ecran.
    const zg=document.querySelector('.souris-wrap.tp .tp-left');
    if(zg){
      clearFlashIv();
      flashClic(zg, 'var(--bleu)');
      window.__flashIv=setInterval(()=>{
        if(current.kind!=='presentationClic'){ clearFlashIv(); return; }
        flashClic(zg, 'var(--bleu)');
      }, 1400);
    }
  }, 6000);
  kompaParleOn();
  let droiteBlanchie=false;
  jouerEcran(4, (t)=>{
    const dur=ecranAudio.duration||0;
    if(!droiteBlanchie && dur && (dur-t)<=5){ droiteBlanchie=true; const r=document.querySelector('.tp-right'); if(r) r.setAttribute('fill','#ffffff'); }
  }, ()=>{ const k=document.getElementById('kompaC'); if(k) k.classList.remove('parle'); allerA(etape+1); });
  refreshEditPanel();
}

/* ÉCRAN 3 — Mission 1 : l'enfant contrôle la vraie souris et doit suivre le papillon sur l'écran du portable. */
function renderMission1(){
  current={kind:'mission1'}; setProgress(1, true); majProg();
  activerSuivi(false); cacheKompa();
  main.innerHTML=`<div class="screen active lap2">
    <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px">Glisse ton doigt sur le trackpad et suis le papillon !</div>
    <div class="lap-wrap">${laptopSVG()}</div>
    <div class="m1-win" id="m1Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">Mission 1 réussie ✓</div></div></div>
    <div class="m1-timer">
      <div id="m1Count">20s</div>
      <div class="m1-clock-wrap" id="m1Clock"><img class="m1-clock gris" src="${window.__resources.reveilImg}" alt="minuteur"><img class="m1-clock coul" id="m1Coul" src="${window.__resources.reveilImg}" alt=""></div>
    </div>
    <span id="m1Num" style="display:none"></span>
  </div>`;
  const svg=main.querySelector('.lap-svg');
  (function positionM1Win(){
    // le message de réussite reste à droite : sa largeur s'adapte pour ne jamais toucher le laptop
    const win=document.getElementById('m1Win'), card=win.querySelector('.m1-win-card');
    const gap=20, cardRight=win.getBoundingClientRect().right - parseFloat(getComputedStyle(win).paddingRight||0);
    const avail=cardRight - (svg.getBoundingClientRect().right + gap);
    card.style.maxWidth=Math.max(60, avail)+'px';
  })();
  const cursor=svg.querySelector('#lapCursor'), hand=svg.querySelector('#lapHand');
  const pap=document.createElementNS('http://www.w3.org/2000/svg','image');
  pap.setAttribute('href',PAP_LOWPOLY); pap.setAttribute('width','60'); pap.setAttribute('height','60'); pap.setAttribute('id','lapPap'); pap.setAttribute('transform','translate(370,168)');
  svg.insertBefore(pap, cursor);
  const ov=document.createElementNS('http://www.w3.org/2000/svg','rect');
  ov.setAttribute('x','152'); ov.setAttribute('y','60'); ov.setAttribute('width','496'); ov.setAttribute('height','276');
  ov.setAttribute('fill','transparent'); ov.style.cursor='none';   // masque le curseur système sur l'écran blanc
  svg.appendChild(ov);
  const pt=svg.createSVGPoint();
  const toVB=(x,y)=>{ pt.x=x; pt.y=y; const m=svg.getScreenCTM(); return m?pt.matrixTransform(m.inverse()):{x:400,y:200}; };
  const DUR=20000; let followMs=0, done=false, lastTick=performance.now(), curCx=400, curCy=198, t0=performance.now();
  const num=document.getElementById('m1Num'), count=document.getElementById('m1Count'), clock=document.getElementById('m1Clock'), coul=document.getElementById('m1Coul'), banner=document.getElementById('ecranBox');
  if(window.lapRaf) cancelAnimationFrame(window.lapRaf);
  const loop=(now)=>{
    const t=(now-t0)/1000;
    const bx=400+200*Math.sin(t*0.55)+30*Math.sin(t*1.3);
    const by=200+108*Math.sin(t*0.9+1.1);
    pap.setAttribute('transform',`translate(${bx-30},${by-30})`);
    const p=toVB(mx,my);
    const inside = p.x>=152 && p.x<=648 && p.y>=60 && p.y<=336;
    if(inside){ curCx=p.x; curCy=p.y;
      cursor.setAttribute('transform',`translate(${curCx-9},${curCy-6})`);
      const fx=(curCx-152)/(648-152), fy=(curCy-60)/(336-60);
      if(hand) hand.setAttribute('transform',`translate(${(340+fx*120)-75},${(446+fy*32)-18})`);
    }
    const moved=(now-lastMoveT)<1200;
    const dist=Math.hypot(curCx-bx, curCy-by);
    const suit = inside && moved && dist<95;
    const dt=now-lastTick; lastTick=now;
    if(!done){
      if(suit){ followMs+=dt; if(clock) clock.classList.remove('alert'); if(banner) banner.classList.remove('clignote'); }
      else { if(clock) clock.classList.add('alert'); if(banner) banner.classList.add('clignote'); }
      const prog=Math.min(1, followMs/DUR);
      if(coul) coul.style.clipPath='inset('+((1-prog)*100)+'% 0 0 0)';
      const remaining=Math.max(0, DUR-followMs);
      if(count) count.textContent=Math.ceil(remaining/1000)+'s';
      if(followMs>=DUR){ done=true; if(clock) clock.classList.remove('alert'); if(banner) banner.classList.remove('clignote'); if(coul) coul.style.clipPath='inset(0 0 0 0)'; if(count) count.textContent='0s'; setProgress(2); if(typeof bipOk==='function') bipOk(); const win=document.getElementById('m1Win'); if(win) win.classList.add('show'); jouerVictoire(2, ()=>allerA(etape+1)); }
    }
    window.lapRaf=requestAnimationFrame(loop);
  };
  jouerEcran(2, null, ()=>{ t0=performance.now(); lastTick=performance.now(); window.lapRaf=requestAnimationFrame(loop); });
}

/* ÉCRAN — Mission 2 : couloir de lanterns à allumer en passant le curseur dessus */
function renderMission2(){
  current={kind:'mission2'}; setProgress(2, true); majProg();
  activerSuivi(false); cacheKompa();
  const paires={
    vert0:[window.__resources.lantVert0Off,window.__resources.lantVert0On],
    bleu:[window.__resources.lantBleuOff,window.__resources.lantBleuOn],
    orange:[window.__resources.lantOrangeOff,window.__resources.lantOrangeOn],
    rose:[window.__resources.lantRoseOff,window.__resources.lantRoseOn],
    vert:[window.__resources.lantVertOff,window.__resources.lantVertOn]
  };
  // 3 lanternes par fil ; le fil part de sous la consigne et descend jusqu'au bord bas ; côté droit symétrique
  const cols={ left:[['rose','64%','24%','40%'],['orange','37%','49%','46%'],['vert0','16%','71%','30%']],
               right:[['bleu','64%','24%','40%'],['vert','37%','49%','46%'],['orange','16%','71%','30%']] };
  const fil='<svg class="fil-v" viewBox="0 0 100 100" preserveAspectRatio="none"><path d="M97,1 C76,12 95,38 60,50 C33,60 28,84 2,99" fill="none" stroke="#111" stroke-width="6" stroke-linecap="round" vector-effect="non-scaling-stroke"/></svg>';
  const garland=(side)=>`<div class="garland ${side}">${fil}${cols[side].map(c=>{const p=paires[c[0]];return `<div class="lan" style="left:${c[1]}; top:${c[2]}; width:${c[3]}"><img data-off="${p[0]}" data-on="${p[1]}" src="${p[0]}" alt="lanterne"></div>`;}).join('')}</div>`;
  const n=cols.left.length+cols.right.length;
  main.innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden">
    <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; margin-bottom:2vh; z-index:30; align-self:center">Passe le curseur sur chaque lanterne pour l'allumer !</div>
    <div class="corridor" id="corridor">${garland('left')}${garland('right')}</div>
    <div class="m1-win" id="m2Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">Mission 2 réussie ✓</div></div></div>
  </div>`;
  let lit=0, done=false;
  const light=(img)=>{ if(img.dataset.done||done)return; img.dataset.done='1'; img.src=img.dataset.on; if(typeof bipClic==='function') bipClic(); lit++;
    if(lit===n){ done=true; setProgress(3); if(typeof bipOk==='function') bipOk(); const w=document.getElementById('m2Win'); if(w) w.classList.add('show'); jouerVictoire(3, ()=>allerA(etape+1)); } };
  jouerEcran(3, null, ()=>{
    main.querySelectorAll('.corridor img').forEach(img=>{
      img.addEventListener('mousemove', e=>{ const r=img.getBoundingClientRect(); const cx=r.left+r.width/2, cy=r.top+r.height*0.42; const rad=Math.min(r.width,r.height)*0.24; if(Math.hypot(e.clientX-cx,e.clientY-cy)<rad) light(img); });
    });
  });
}

/* ÉCRAN — Mission 3 : ouvrir la porte rouge avec un vrai clic gauche */
function renderMission3(){
  current={kind:'mission3'}; setProgress(3, true); majProg();
  activerSuivi(false); cacheKompa();
  main.innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden">
    <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; margin-bottom:2vh; z-index:5">Pose le curseur sur la serrure et fais un clic gauche pour ouvrir la porte !</div>
    ${kompaCentre()}
    <div class="porte3-wrap" id="porte3Wrap" style="opacity:0; transition:opacity .5s ease">
      <img class="porte3" id="porte3" src="${window.__resources.porteRougeImg}" alt="porte rouge">
      <div class="porte3-serrure" id="porte3Serrure" title="serrure"></div>
    </div>
    <div class="m1-win" id="m3Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">Mission 3 réussie ✓</div></div></div>
  </div>`;
  kompaParleOn();
  let done=false, actif=false;
  const porte=document.getElementById('porte3'), serrure=document.getElementById('porte3Serrure');
  const porteWrap=document.getElementById('porte3Wrap');
  const consBox=document.getElementById('ecranBox');
  let consTO=null;
  serrure.addEventListener('click', ()=>{ if(!actif||done)return; done=true; clearTimeout(consTO); if(consBox) consBox.classList.remove('cons-alt');
    porte.src=window.__resources.porteSourisImg; porte.classList.add('ouverte'); serrure.style.display='none';
    setProgress(4); if(typeof bipOk==='function') bipOk();
    const w=document.getElementById('m3Win');
    setTimeout(()=>{ if(w) w.classList.add('show'); }, 1100);
    jouerVictoire(5, ()=>allerA(etape+1));
  });
  jouerEcran(5, null, ()=>{ cacheKompa(); if(porteWrap) porteWrap.style.opacity='1'; actif=true; consTO=setTimeout(()=>{ if(!done&&consBox) consBox.classList.add('cons-alt'); }, 3000); });
}

/* ÉCRAN — Mission 4 : ouvrir 5 portes, chacune révèle un personnage différent */
const M4_PORTES=[
  window.__resources.porteLapinImg,
  window.__resources.porteChapelierImg,
  window.__resources.porteChatImg,
  window.__resources.porteFlamantImg,
  window.__resources.porteSoldatImg
];
function renderMission4(){
  current={kind:'mission4'}; setProgress(4, true); majProg();
  activerSuivi(false); cacheKompa();
  main.innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden">
    <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; margin-bottom:2vh; z-index:5">Clique gauche sur la serrure de chaque porte pour l'ouvrir !</div>
    ${kompaCentre()}
    <div class="portes4-field" id="portes4Field" style="opacity:0; transition:opacity .5s ease"></div>
    <div class="porte4-msg" id="porte4Msg"></div>
    <div class="m1-win" id="m4Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">Toutes les portes ouvertes ✓</div></div></div>
  </div>`;
  kompaParleOn();
  let reussies=0, done=false, actif=false;
  const consBox=document.getElementById('ecranBox'), field=document.getElementById('portes4Field');
  let consTO=null, hideTO=null;
  const armCons=()=>{ clearTimeout(consTO); if(consBox) consBox.classList.remove('cons-alt'); consTO=setTimeout(()=>{ if(!done&&consBox) consBox.classList.add('cons-alt'); }, 3000); };
  field.addEventListener('contextmenu', e=>{ e.preventDefault(); if(done)return; done=true; clearTimeout(consTO); clearTimeout(hideTO); allerA(etape-1); });
  const BESOIN=5; let idx=0, lastZone=-1;
  const spawn=()=>{
    if(done)return;
    const fw=field.clientWidth, fh=field.clientHeight, pw=Math.min(300,fw*0.32), ph=pw*1.25;
    const src=M4_PORTES[idx%M4_PORTES.length]; idx++;
    const cell=document.createElement('div'); cell.className='porte4';
    // répartir sur l'écran : grille 3x2, zone différente de la précédente
    const cols=3, rows=2, nz=cols*rows; let z=Math.floor(Math.random()*nz); if(z===lastZone) z=(z+1)%nz; lastZone=z;
    const zc=z%cols, zr=Math.floor(z/cols); const cw=(fw-pw)/(cols-1||1), chh=(fh-ph)/(rows-1||1);
    const jitterX=(Math.random()-0.5)*cw*0.5, jitterY=(Math.random()-0.5)*chh*0.5;
    const x=Math.max(0,Math.min(fw-pw, zc*cw+jitterX)), y=Math.max(0,Math.min(fh-ph, zr*chh+jitterY));
    cell.style.cssText=`position:absolute; left:${x}px; top:${y}px; width:${pw}px`;
    cell.innerHTML=`<img class="porte4-img" src="${window.__resources.porteRougeImg}" data-open="${src}" alt="porte"><div class="porte4-serrure" title="serrure"></div>`;
    const img=cell.querySelector('.porte4-img'), ser=cell.querySelector('.porte4-serrure');
    field.appendChild(cell); armCons();
    hideTO=setTimeout(()=>{ if(img.dataset.done)return; cell.remove(); spawn(); }, 2000);   // pas cliquée à temps -> disparaît
    const ouvrir=ev=>{ if(!actif)return; if(ev.button!==0)return; if(img.dataset.done||done)return; img.dataset.done='1'; clearTimeout(hideTO); armCons();
      img.src=img.dataset.open; img.classList.add('ouverte'); ser.style.display='none'; if(typeof bipClic==='function') bipClic(); reussies++;
      if(reussies>=BESOIN){ done=true; clearTimeout(consTO); if(consBox) consBox.classList.remove('cons-alt'); setProgress(5); if(typeof bipOk==='function') bipOk();
        const w=document.getElementById('m4Win'); setTimeout(()=>{ if(w) w.classList.add('show'); }, 900);
        jouerVictoire(6, ()=>allerA(etape+1)); }
      else { const msg=document.getElementById('porte4Msg');
        if(msg){ msg.textContent = reussies===1 ? 'Première porte ouverte' : (reussies===4 ? 'One to go !' : 'Trop bien ! Encore une'); msg.classList.add('show'); setTimeout(()=>msg.classList.remove('show'), 1100); }
        setTimeout(()=>{ cell.remove(); spawn(); }, 1200); }
    };
    cell.addEventListener('click', ouvrir);
  };
  jouerEcran(6, null, ()=>{ cacheKompa(); if(field) field.style.opacity='1'; actif=true; armCons(); requestAnimationFrame(spawn); });
}

/* Slide de victoire (fin de notion) — juste la médaille (sans cadre) + record en haut à droite */
function renderBadge(){
  current={kind:'badge'}; setProgress(5); majProg(); const B=CONFIG.badge;
  setHeaderTools(false); // masque Son + Retour sur la victoire
  const confettis=Array.from({length:14}).map((_,k)=>{
    const left=6+k*6.5, delay=(k%7)*0.32, emo=['🎉','✨','⭐','🎊','🌟'][k%5];
    return `<div class="conf" style="left:${left}%;animation-delay:${delay}s">${emo}</div>`;
  }).join('');
  main.innerHTML=`<div class="screen active">
    <div class="victoire">
      ${confettis}
      <div class="medaille-img" style="font-size:clamp(70px,12vw,130px)">🏅</div>
      <h1 id="titre">${U.titreVictoire}</h1>
      <div class="kompa-bulle sans-queue" style="position:static; white-space:pre-line; max-width:min(640px,64vw)">${B.ecran}</div>
    </div>
  </div>`;
  els={ titre:document.getElementById('titre') };
  bipOk();
  pqSetFait('A');   // niveau A validé → débloque la suite dans le parcours
  jouerEcran(7, null, null);
}

/* (Mode édition retiré) */
function refreshEditPanel(){}

/* ===== PAGE D'ACCUEIL : parcours des niveaux ===== */
const PQ_NIVEAUX = [
  { id:"A", lettre:"A", titre:"Déplacement & clic gauche", couleur:"var(--bleu)",   x:30, y:90, dispo:true },
  { id:"B", lettre:"B", titre:"Maintien & glisser-déposer", couleur:"var(--rouge)",  x:72, y:66, dispo:true },
  { id:"C", lettre:"C", titre:"Clic droit & molette",        couleur:"var(--orange)", x:28, y:40, dispo:true }
];
const PQ_PERMIS = { lettre:"Permis", titre:"Récompense finale", x:62, y:12 };
const PQ_SEGS = [
  { d:"M 24 97 C 24 93 27 91 30 90", arr:0 },
  { d:"M 30 90 C 42 86 66 82 72 66", arr:1 },
  { d:"M 72 66 C 80 50 20 56 28 40", arr:2 },
  { d:"M 28 40 C 36 26 56 26 62 12", arr:3 }
];
function pqGetFaits(){ try{ const r=localStorage.getItem('permisNiveauxFaits'); if(r){ const a=JSON.parse(r); if(Array.isArray(a)) return a; } }catch(e){} return []; }
function pqSetFait(id){ try{ const a=pqGetFaits(); if(!a.includes(id)){ a.push(id); localStorage.setItem('permisNiveauxFaits', JSON.stringify(a)); } }catch(e){} }
function pqNbFaits(){ const f=pqGetFaits(); return PQ_NIVEAUX.filter(n=>f.includes(n.id)).length; }
function pqTousFaits(){ const f=pqGetFaits(); return PQ_NIVEAUX.every(n=>f.includes(n.id)); }
function pqEtatNiveau(n){
  const f=pqGetFaits();
  if(f.includes(n.id)) return 'valide';
  // Enchainement impose : on ne joue B que si A est valide, C que si B l'est.
  const idx=PQ_NIVEAUX.findIndex(x=>x.id===n.id);
  const precedentFait = idx<=0 || f.includes(PQ_NIVEAUX[idx-1].id);
  return (n.dispo && precedentFait) ? 'encours' : 'verrou';
}

const pqEl=document.getElementById('parcours');
const pqNodes=document.getElementById('pqNodes');
const pqSegEl=document.getElementById('pqSegments');
const pqBadges=document.getElementById('pqBadges');
const pqCompteurNb=document.getElementById('pqCompteurNb');
const pqCardTitre=document.getElementById('pqCardTitre');
const pqCardEtat=document.getElementById('pqCardEtat');
const pqKompaBody=document.getElementById('pqKompaBody');
const pqHomeBtn=document.getElementById('pqHomeBtn');
const pqLogo=document.getElementById('pqLogo');

function pqToast(msg){ const t=document.getElementById('pq-toast'); if(!t)return; t.textContent=msg; t.classList.add('show'); clearTimeout(t._t); t._t=setTimeout(()=>t.classList.remove('show'),2200); }

function pqRendreSegments(){
  pqSegEl.innerHTML='';
  const faits=pqGetFaits();
  let idxEncours=-1;
  for(let i=0;i<PQ_NIVEAUX.length;i++){ if(!faits.includes(PQ_NIVEAUX[i].id)){ idxEncours=i; break; } }
  PQ_SEGS.forEach(s=>{
    let atteint;
    if(s.arr<3){ const n=PQ_NIVEAUX[s.arr]; atteint = faits.includes(n.id) || (s.arr===idxEncours); }
    else { atteint = pqTousFaits(); }
    const core=document.createElementNS('http://www.w3.org/2000/svg','path');
    core.setAttribute('d',s.d); core.setAttribute('fill','none');
    core.setAttribute('stroke', atteint?'var(--rouge)':'var(--grisroute)');
    core.setAttribute('stroke-width','9'); core.setAttribute('stroke-linecap','round'); core.setAttribute('stroke-linejoin','round'); core.setAttribute('vector-effect','non-scaling-stroke');
    pqSegEl.appendChild(core);
    const band=document.createElementNS('http://www.w3.org/2000/svg','path');
    band.setAttribute('d',s.d); band.setAttribute('fill','none'); band.setAttribute('stroke','#ffffff');
    band.setAttribute('stroke-width','9'); band.setAttribute('stroke-linecap','butt'); band.setAttribute('stroke-dasharray','3 7'); band.setAttribute('vector-effect','non-scaling-stroke'); band.setAttribute('opacity','.85');
    pqSegEl.appendChild(band);
  });
}

function pqNoeud(data, etat, isPermis){
  const div=document.createElement('div');
  div.className='pq-node '+etat+(isPermis?' permis':'');
  div.style.left=data.x+'%'; div.style.top=data.y+'%';
  let rondInner, cta='';
  if(isPermis){
    rondInner = (etat==='gagne')
      ? '<img class="pq-permis-medaille" src="assets/images/medaille.webp" alt="Permis de la Souris" style="width:72%; height:auto; display:block; margin:auto">'
      : '<span class="pq-permis-emoji" style="filter:grayscale(1); opacity:.55">🔒</span>';
  } else {
    const coche=etat==='valide'?'<span class="pq-coche">✓</span>':'';
    const cadenas=etat==='verrou'?'<span class="pq-cadenas">🔒</span>':'';
    rondInner = `<span class="pq-lvl">Niveau</span><span class="pq-lettre">${data.lettre}</span>${coche}${cadenas}`;
    if(etat==='encours') cta='<button class="pq-cta">▶ Commencer</button>';
    else if(etat==='valide') cta='<button class="pq-cta">↺ Rejouer</button>';
  }
  div.innerHTML=`
    <div class="pq-rond" role="button" tabindex="0" ${isPermis?'':`style="background:${data.couleur}"`}>
      ${rondInner}
    </div>
    ${cta}`;
  const go=()=>pqActiver(data, etat, div, isPermis);
  const rond=div.querySelector('.pq-rond');
  rond.addEventListener('click', go);
  rond.addEventListener('keydown', e=>{ if(e.key==='Enter'||e.key===' '){ e.preventDefault(); go(); } });
  const b=div.querySelector('.pq-cta'); if(b) b.addEventListener('click', e=>{ e.stopPropagation(); go(); });
  return div;
}

function renderParcours(){
  if(pqLogo && !pqLogo.firstChild){ const appLogo=document.querySelector('header .logo svg'); if(appLogo){ const c=appLogo.cloneNode(true); c.removeAttribute('id'); c.querySelectorAll('[id]').forEach(e=>e.removeAttribute('id')); pqLogo.appendChild(c); } }
  if(pqKompaBody && typeof KOMPA_SRC!=='undefined' && KOMPA_SRC){ pqKompaBody.innerHTML=kompaEl('', null, 'parcours'); }
  pqRendreSegments();
  pqNodes.innerHTML='';
  PQ_NIVEAUX.forEach(n=> pqNodes.appendChild(pqNoeud(n, pqEtatNiveau(n), false)));
  pqNodes.appendChild(pqNoeud(PQ_PERMIS, pqTousFaits()?'gagne':'attente', true));
  const nb=pqNbFaits();
  if(pqCompteurNb) pqCompteurNb.textContent=nb+' / 3';
  if(pqBadges){ pqBadges.innerHTML=''; for(let k=0;k<3;k++){ const p=document.createElement('span'); p.className='pq-pastille'+(k<nb?' ok':''); pqBadges.appendChild(p); } }
  // carte d'état : bandeau = niveau actuel, corps = avancement
  const faits=pqGetFaits();
  const courant=PQ_NIVEAUX.find(n=>!faits.includes(n.id));
  if(!courant){ pqCardTitre.textContent='Le Permis'; pqCardEtat.textContent='Permis obtenu — bravo, les Kompagnons !'; }
  else {
    pqCardTitre.textContent='Niveau '+courant.lettre;
    const etatC=pqEtatNiveau(courant);
    if(etatC==='verrou'){
      const idxC=PQ_NIVEAUX.findIndex(x=>x.id===courant.id);
      const precC=idxC>0 ? PQ_NIVEAUX[idxC-1].lettre : null;
      pqCardEtat.textContent = precC ? ('Termine le niveau '+precC+' pour débloquer !') : 'Bientôt disponible';
    } else {
      pqCardEtat.textContent='Clique sur la pastille pour démarrer !';
    }
  }
}

function pqActiver(data, etat, div, isPermis){
  if(isPermis){
    if(etat==='gagne'){ pfOuvrir(); }
    else { div.classList.remove('pq-secoue'); void div.offsetWidth; div.classList.add('pq-secoue'); pqToast('🏅 Termine les 3 niveaux pour gagner ton Permis !'); }
    return;
  }
  if(etat==='verrou'){
    div.classList.remove('pq-secoue'); void div.offsetWidth; div.classList.add('pq-secoue');
    const idxV=PQ_NIVEAUX.findIndex(x=>x.id===data.id);
    const prec=idxV>0 ? PQ_NIVEAUX[idxV-1].lettre : null;
    pqToast(prec ? `🔒 Termine d'abord le niveau ${prec} !` : `🔒 Niveau ${data.lettre} arrive bientôt !`);
    return;
  }
  if(data.id==='A') lancerNiveauA();
  else if(data.id==='B') lancerNiveauB();
  else if(data.id==='C') lancerNiveauC();
}

/* --- Musique de fond du parcours --- */
let MUSIQUE_B64 = "";   /* ← colle ici le base64 d'un MP3 (en boucle) ; vide = petite musique de secours synthétisée */
let musiqueAudio=null, musiqueActive=false, musiqueTimer=null, musiqueSynthOn=false, musiqueDucked=false;
const MUS_VOL={normal:0.35, duck:0.06}, MUS_SYNTH={normal:0.1, duck:0.02};
function musiqueDuck(on){ musiqueDucked=on; if(musiqueAudio){ try{ musiqueAudio.volume = on?MUS_VOL.duck:MUS_VOL.normal; }catch(e){} } }
function musiqueInit(){ if(MUSIQUE_B64 && !musiqueAudio){ try{ musiqueAudio=new Audio('data:audio/mpeg;base64,'+MUSIQUE_B64); musiqueAudio.loop=true; musiqueAudio.volume=musiqueDucked?MUS_VOL.duck:MUS_VOL.normal; }catch(e){} } }
const PQ_MELODIE=[523.25,659.25,783.99,659.25,880.00,783.99,659.25,587.33,523.25,659.25,587.33,523.25,440.00,523.25,587.33,659.25];
const PQ_BASSE={0:130.81,4:196.00,8:220.00,12:196.00}; let pqStep=0;
function pqSynthNote(freq,dur,vol,type){ if(!AC) return; try{ const o=AC.createOscillator(), g=AC.createGain(); o.type=type||'triangle'; o.frequency.value=freq; const t=AC.currentTime; g.gain.setValueAtTime(0,t); g.gain.linearRampToValueAtTime(vol,t+0.012); g.gain.exponentialRampToValueAtTime(0.0001,t+dur); o.connect(g); g.connect(AC.destination); o.start(t); o.stop(t+dur+0.05); }catch(e){} }
function pqSynthStart(){ if(musiqueSynthOn) return; musiqueSynthOn=true; try{ AC=AC||new (window.AudioContext||window.webkitAudioContext)(); if(AC.state==='suspended') AC.resume(); }catch(e){} pqStep=0;
  musiqueTimer=setInterval(()=>{ if(!sonOn||!musiqueActive) return; const v=musiqueDucked?MUS_SYNTH.duck:MUS_SYNTH.normal; const i=pqStep%PQ_MELODIE.length; pqSynthNote(PQ_MELODIE[i],0.22,v,'triangle'); if(PQ_BASSE[i]!=null) pqSynthNote(PQ_BASSE[i],0.42,v*1.1,'sine'); pqStep++; }, 200); }
function pqSynthStop(){ musiqueSynthOn=false; if(musiqueTimer){ clearInterval(musiqueTimer); musiqueTimer=null; } }
function musiqueStart(){ musiqueStop(); /* musique désactivée */ }
function musiqueStop(){ musiqueActive=false; if(musiqueAudio){ try{ musiqueAudio.pause(); }catch(e){} } pqSynthStop(); }
/* reprise au 1er geste (politique d'autoplay des navigateurs) */
(function(){ const h=()=>{ if(musiqueActive){ try{ if(AC&&AC.state==='suspended') AC.resume(); }catch(e){} if(MUSIQUE_B64&&musiqueAudio){ const p=musiqueAudio.play(); if(p&&p.catch) p.catch(()=>{}); } } }; document.addEventListener('pointerdown',h); document.addEventListener('keydown',h); })();

function ouvrirParcours(){
  if(window.NB && window.NB.stop) window.NB.stop();
  if(window.NC && window.NC.stop) window.NC.stop();
  clearInactivite(); clearTempo(); sinonCourant=null;
  if(libreIv){ clearInterval(libreIv); libreIv=null; }
  if(papRaf){ cancelAnimationFrame(papRaf); papRaf=null; }
  if(window.speechSynthesis) speechSynthesis.cancel(); stopOral(); activerSuivi(false);
  renderParcours(); pqEl.classList.add('on'); musiqueStart(); musiqueDuck(false);
  installerBoutonAdmin();
}

/* ===== Réinitialisation (encadrant) =====
   Discret et présent uniquement sur la page d'accueil du parcours, pour qu'un enfant
   ne tombe pas dessus en cours de partie. Le code est écrit dans ce fichier : il dissuade
   un enfant curieux, ce n'est pas une sécurité au sens strict. */
const ADMIN_CODE="kompa2026";
function installerBoutonAdmin(){
  if(!pqEl || document.getElementById('pqAdminBtn')) return;
  const b=document.createElement('button');
  b.id='pqAdminBtn'; b.type='button'; b.textContent='⚙';
  b.title="Réinitialiser la progression (encadrant)";
  b.setAttribute('aria-label',"Réinitialiser la progression (encadrant)");
  b.style.cssText='position:absolute; left:10px; bottom:10px; z-index:40; width:34px; height:34px;'
    +'border-radius:50%; border:2px solid #b9c6d6; background:#fff; color:#7d8a99; font-size:16px;'
    +'line-height:1; cursor:pointer; opacity:.35; transition:opacity .2s ease; padding:0;';
  b.addEventListener('mouseenter', ()=>{ b.style.opacity='1'; });
  b.addEventListener('mouseleave', ()=>{ b.style.opacity='.35'; });
  b.addEventListener('click', ()=>{
    const saisie=prompt("Code encadrant pour réinitialiser la progression :");
    if(saisie===null) return;                       // annulé
    if(saisie!==ADMIN_CODE){ pqToast("Code incorrect."); return; }
    if(!confirm("Effacer la progression et recommencer au niveau A ?")) return;
    try{ localStorage.removeItem('permisNiveauxFaits'); localStorage.removeItem('permisDebut'); localStorage.removeItem('permisTempsFinal'); localStorage.removeItem('permisEleveInfo'); }catch(e){}
    renderParcours(); pqToast("Progression réinitialisée.");
  });
  pqEl.appendChild(b);
}
function debloqueAudio(){
  try{ AC=AC||new (window.AudioContext||window.webkitAudioContext)(); if(AC.state==='suspended') AC.resume(); }catch(e){}
  if('speechSynthesis' in window){ try{ speechSynthesis.resume(); const w=new SpeechSynthesisUtterance('Go'); w.lang='fr-FR'; w.volume=0; speechSynthesis.speak(w); }catch(e){} }
}

/* ===== PAGE FINALE : obtention du Permis de la Souris ===== */
const PF_DEBUT_KEY='permisDebut', PF_TEMPS_KEY='permisTempsFinal', PF_INFO_KEY='permisEleveInfo', PF_REGISTRE_KEY='permisRegistre';
const permisFinalEl=document.getElementById('permisFinal');
const pfLogo=document.getElementById('pfLogo');
const pfForm=document.getElementById('pfForm');
const pfCongrats=document.getElementById('pfCongrats');
const pfPrenomInp=document.getElementById('pfPrenom');
const pfNomInp=document.getElementById('pfNom');
const pfValiderBtn=document.getElementById('pfValiderBtn');
const pfMsgEl=document.getElementById('pfMsg');
const pfRetourBtn=document.getElementById('pfRetourBtn');
const pfAppelEl=document.getElementById('pfAppel');
const pfIntervenantBtn=document.getElementById('pfIntervenantBtn');
const pfTempsBox=document.getElementById('pfTempsBox');
const pfTempsVal=document.getElementById('pfTempsVal');

function pfMarquerDebut(){
  try{ if(pqNbFaits()===0 && !localStorage.getItem(PF_DEBUT_KEY)) localStorage.setItem(PF_DEBUT_KEY, String(Date.now())); }catch(e){}
}
function pfFormatTemps(ms){
  const s=Math.max(0,Math.round(ms/1000)), mm=Math.floor(s/60), ss=s%60;
  return String(mm).padStart(2,'0')+':'+String(ss).padStart(2,'0');
}
function pfMarquerFin(){
  try{
    if(localStorage.getItem(PF_TEMPS_KEY)) return;
    const debut=parseInt(localStorage.getItem(PF_DEBUT_KEY)||'',10);
    const ms=isNaN(debut)?0:(Date.now()-debut);
    localStorage.setItem(PF_TEMPS_KEY, pfFormatTemps(ms));
  }catch(e){}
}
function pfEnregistrer(nom, prenom){
  const temps=(function(){ try{ return localStorage.getItem(PF_TEMPS_KEY)||'00:00'; }catch(e){ return '00:00'; } })();
  const ligne=`${nom} ${prenom} — Temps d'exécution : ${temps}`;
  try{
    const r=localStorage.getItem(PF_REGISTRE_KEY); const a=r?JSON.parse(r):[];
    a.push(ligne); localStorage.setItem(PF_REGISTRE_KEY, JSON.stringify(a));
    localStorage.setItem(PF_INFO_KEY, JSON.stringify({nom:nom, prenom:prenom, temps:temps}));
  }catch(e){}
  return {temps:temps, ligne:ligne};
}
function pfAfficherCongrats(prenom){
  pfForm.style.display='none';
  pfCongrats.style.display='flex';
  pfMsgEl.textContent=`Bravo, ${prenom} tu as obtenu ton permis de la souris ! Master Mouse !`;
  if(pfAppelEl) pfAppelEl.style.display='';
  if(pfIntervenantBtn) pfIntervenantBtn.style.display='';
  if(pfTempsBox) pfTempsBox.style.display='none';
}
if(pfValiderBtn) pfValiderBtn.addEventListener('click', ()=>{
  const prenom=(pfPrenomInp.value||'').trim(), nom=(pfNomInp.value||'').trim();
  if(!prenom || !nom){ pqToast('Merci de renseigner ton nom et ton prénom.'); return; }
  bipOk();
  pfEnregistrer(nom, prenom);
  pfAfficherCongrats(prenom);
});
if(pfIntervenantBtn) pfIntervenantBtn.addEventListener('click', ()=>{
  bipOk();
  let temps='00:00'; try{ temps=localStorage.getItem(PF_TEMPS_KEY)||'00:00'; }catch(e){}
  if(pfTempsVal) pfTempsVal.textContent=temps;
  if(pfTempsBox) pfTempsBox.style.display='flex';
  if(pfAppelEl) pfAppelEl.style.display='none';
  pfIntervenantBtn.style.display='none';
});
if(pfRetourBtn) pfRetourBtn.addEventListener('click', ()=>{ bipClic(); if(permisFinalEl) permisFinalEl.classList.remove('on'); ouvrirParcours(); });
function pfTelechargerRegistre(){
  let lignes=[]; try{ const r=localStorage.getItem(PF_REGISTRE_KEY); if(r) lignes=JSON.parse(r); }catch(e){}
  const blob=new Blob([lignes.join('\n')+'\n'], {type:'text/plain'});
  const url=URL.createObjectURL(blob);
  const a=document.createElement('a'); a.href=url; a.download='registre-permis-souris.txt';
  document.body.appendChild(a); a.click(); a.remove();
  setTimeout(()=>URL.revokeObjectURL(url), 1000);
}
function installerBoutonAdminPF(){
  if(!permisFinalEl || document.getElementById('pfAdminBtn')) return;
  const b=document.createElement('button');
  b.id='pfAdminBtn'; b.type='button'; b.textContent='⬇';
  b.title="Télécharger le registre des élèves (intervenant)";
  b.setAttribute('aria-label',"Télécharger le registre des élèves (intervenant)");
  b.style.cssText='position:absolute; left:10px; bottom:10px; z-index:40; width:34px; height:34px;'
    +'border-radius:50%; border:2px solid #b9c6d6; background:#fff; color:#7d8a99; font-size:16px;'
    +'line-height:1; cursor:pointer; opacity:.35; transition:opacity .2s ease; padding:0;';
  b.addEventListener('mouseenter', ()=>{ b.style.opacity='1'; });
  b.addEventListener('mouseleave', ()=>{ b.style.opacity='.35'; });
  b.addEventListener('click', ()=>{
    const saisie=prompt("Code encadrant pour télécharger le registre :");
    if(saisie===null) return;
    if(saisie!==ADMIN_CODE){ pqToast("Code incorrect."); return; }
    pfTelechargerRegistre();
  });
  permisFinalEl.appendChild(b);
}
function pfOuvrir(){
  if(window.NC && window.NC.stop) window.NC.stop();
  if(window.NB && window.NB.stop) window.NB.stop();
  if(pqEl) pqEl.classList.remove('on');
  pfMarquerFin();
  installerBoutonAdminPF();
  if(pfLogo && !pfLogo.firstChild){ const appLogo=document.querySelector('header .logo svg'); if(appLogo){ const c=appLogo.cloneNode(true); c.removeAttribute('id'); c.querySelectorAll('[id]').forEach(e=>e.removeAttribute('id')); pfLogo.appendChild(c); } }
  let info=null; try{ const r=localStorage.getItem(PF_INFO_KEY); if(r) info=JSON.parse(r); }catch(e){}
  if(info && info.prenom){ pfAfficherCongrats(info.prenom); }
  else { pfForm.style.display='flex'; pfCongrats.style.display='none'; if(pfPrenomInp) pfPrenomInp.value=''; if(pfNomInp) pfNomInp.value=''; }
  if(permisFinalEl) permisFinalEl.classList.add('on');
}
function lancerNiveauA(){
  debloqueAudio(); musiqueStart(); musiqueDuck(true);
  pqEl.classList.remove('on');
  pfMarquerDebut();
  bipClic(); allerA(0);
}
if(pqHomeBtn) pqHomeBtn.onclick=()=>{ bipClic(); ouvrirParcours(); };

/* ===================== NIVEAU B (intégré, module isolé) ===================== */
const NB = (function(){
  const CFG = {
    titreNotion:"Le maintien du clic gauche",
    presentation:{
      oral:"Re-bonjour les Kopains, c'est le Professeur Pile Poil ! Vous savez déjà déplacer le curseur et faire un petit clic bleu, alors aujourd'hui on passe au niveau au-dessus : on va apprendre à garder le bouton bleu appuyé. Le truc, c'est ça : on appuie, et on ne relâche pas, on garde le doigt enfoncé pendant qu'on bouge la main. Comme si on attrapait un objet pour le déplacer, puis qu'on le posait où on veut. Attention, le piège, c'est de lever le doigt trop tôt : l'objet vous échappe ! Prêts à devenir des pros de l'attrape-et-pose ?",
      ecran:"On garde le bouton bleu appuyé pour attraper et déplacer."
    },
    demo:{
      oral:"Regardez bien : le doigt gauche garde le clic bleu appuyé, pendant que le doigt droit glisse pour déplacer l'objet !",
      ecran:"Le doigt gauche maintient le clic bleu, le doigt droit glisse pour déplacer l'objet !"
    },
    mission1:{
      titre:"Sors le papillon du labyrinthe",
      consigne:"Pour s'échauffer, on retrouve notre papillon du niveau A ! Cette fois, il s'est perdu dans le labyrinthe du jardin de la Reine. Pose le curseur dessus, appuie sur le bouton bleu et garde-le appuyé : le papillon s'accroche à ta souris. Maintenant, guide-le doucement dans les allées jusqu'à la sortie, sans jamais lâcher le bouton. Attention aux murs, on ne passe pas à travers ! Game Start !",
      ecran:"Garde le bouton bleu appuyé et conduis le papillon jusqu'à la sortie !",
      reussi:"Bravo, le papillon est sorti du labyrinthe !"
    },
    mission2:{
      titre:"Remplis la fiole",
      consigne:"Super les Kompagnons ! Voici une fiole magique, mais elle est vide. Pour la remplir, pose le curseur dessus et garde le bouton bleu appuyé, sans bouger, juste tenir. Plus tu tiens, plus elle se remplit. Ne lâche pas trop tôt !",
      ecran:"Garde le bouton bleu appuyé sur la fiole pour la remplir.",
      reussi:"Voilà une fiole pleine à ras bord, et un maintien parfait ! Tu tiens le bouton comme un champion."
    },
    mission3:{
      titre:"Range le goûter du Chapelier",
      consigne:"Derrière cette porte, c'est la pagaille : le goûter du Chapelier est tout en désordre ! Attrape chaque objet et pose-le sur sa place sur le présentoir. On garde le bouton bleu appuyé à chaque fois !",
      ecran:"Glisse chaque objet à sa place sur le présentoir !",
      reussi:"Magnifique, tout est en place ! Le Chapelier peut servir le thé."
    },
    badge:{
      oral:"Et badge débloqué ! Tu sais maintenant garder le bouton bleu appuyé pour attraper, déplacer et déposer tes objets. Une épreuve de plus, et le Permis de la Souris est à toi ! Prochaine et dernière étape : le bouton orange, le clic droit, et la molette !",
      ecran:"Badge débloqué : maintien et glisser-déposé maîtrisés !\n\nProchaine mission : le clic droit (bouton orange) et la molette."
    }
  };

  /* Voix off (mp3) — squelette en attente des fichiers réels (cf. LISEZ-MOI).
     Un clip par écran (présentation, démo, m1, m2, m3, badge) + un clip de victoire par mission.
     Tant qu'une case vaut null, l'écran reste silencieux (pas de minuteur : la revue est manuelle). */
  const NB_AUDIO=[null,null,null,null,null,null];
  const NB_VICTOIRE=[null,null,null];
  const nbAudioEl=new Audio(); nbAudioEl.preload='auto';
  const nbVicEl=new Audio(); nbVicEl.preload='auto';
  function jouerB(i, onEnd){
    const src=NB_AUDIO[i];
    // Tant qu'aucun audio n'est fourni, on NE declenche PAS l'enchainement : la nav Prec./Suiv. reste la main.
    if(!src||!sonOn) return;
    try{
      nbAudioEl.pause(); nbAudioEl.currentTime=0; nbAudioEl.onended=null;
      nbAudioEl.src=src;
      if(onEnd) nbAudioEl.onended=()=>onEnd();
      const p=nbAudioEl.play();
      if(p&&p.catch) p.catch(()=>{ if(onEnd) bTempo(1200, onEnd); });
    }catch(e){ if(onEnd) bTempo(1200, onEnd); }
  }
  function jouerVictoireB(i, onEnd){
    const src=NB_VICTOIRE[i];
    if(!src||!sonOn) return;
    try{
      nbVicEl.pause(); nbVicEl.currentTime=0; nbVicEl.onended=null;
      nbVicEl.src=src;
      if(onEnd) nbVicEl.onended=()=>onEnd();
      const p=nbVicEl.play();
      if(p&&p.catch) p.catch(()=>{ if(onEnd) bTempo(1200, onEnd); });
    }catch(e){ if(onEnd) bTempo(1200, onEnd); }
  }
  function bAudioPret(){ return NB_AUDIO.some(Boolean); }
  function bSuite(){ if(bAudioPret()) bAllerA(bEtape+1); }

  let bEtape=0, bRaf=null, bTempoId=null;
  function mainEl(){ return document.getElementById('main'); }
  function clearB(){ if(bRaf){cancelAnimationFrame(bRaf);bRaf=null;} if(bTempoId){clearTimeout(bTempoId);bTempoId=null;} if(window.lapRaf){cancelAnimationFrame(window.lapRaf); window.lapRaf=null;} }
  function bTempo(ms,fn){ if(bTempoId)clearTimeout(bTempoId); bTempoId=setTimeout(fn,ms); }
  function majProgB(n, active){
    setProgress(n, active);
    const l=document.getElementById('progLabel'); if(l){ l.textContent='NIVEAU B'; l.style.color='#4A4A4A'; }
    const n4=document.querySelector('#steps .snode[data-i="4"]'); if(n4) n4.style.display='none';
  }

  /* ===== Écran 1 — présentation de la notion (Kompa décentré, comme au niveau A) ===== */
  function bPresentation(){
    current={kind:'presentationB'}; majProgB(0);
    mainEl().innerHTML=`<div class="screen active">
      <h1 id="titre">${CFG.titreNotion}</h1>
      <div id="apres" class="panneau-droit" style="opacity:1">
        <div class="legende">
          <div class="souris-wrap tp">
            ${trackpadHTML({leftOnly:true})}
            <div class="clic-effet">
              <span class="clic-zone"></span><span class="clic-ring"></span><span class="clic-ring r2"></span>
              <img class="main-tap" src="${window.__resources.doigtGauche}" alt="doigt" style="width:clamp(80px,11vw,150px); height:auto">
            </div>
          </div>
        </div>
        <div class="ecran" id="ecranBox" style="margin-top:14px; font-size:clamp(14px,1.5vw,19px); padding:10px 14px">${CFG.presentation.ecran}</div>
      </div>
      ${kompaCentre('gauche')}
    </div>`;
    kompaParleOn();
    jouerB(0);
  }

  /* ===== Écran 2 — démo sur le trackpad : doigt gauche maintient, doigt droit glisse ===== */
  function bDemo(){
    current={kind:'demoB'}; majProgB(0);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active lap2">
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px">${CFG.demo.ecran}</div>
      <div class="lap-wrap">${laptopSVG()}</div>
    </div>`;
    const svg=mainEl().querySelector('.lap-svg');
    const handR=svg.querySelector('#lapHand'), cursor=svg.querySelector('#lapCursor');
    handR.setAttribute('href', window.__resources.doigtDroit); handR.setAttribute('width','66'); handR.setAttribute('height','82');
    const zoneB=document.createElementNS('http://www.w3.org/2000/svg','rect');
    zoneB.setAttribute('x','300'); zoneB.setAttribute('y','472'); zoneB.setAttribute('width','100'); zoneB.setAttribute('height','14'); zoneB.setAttribute('fill','#005da8');
    svg.insertBefore(zoneB, handR);
    const zoneO=document.createElementNS('http://www.w3.org/2000/svg','rect');
    zoneO.setAttribute('x','400'); zoneO.setAttribute('y','472'); zoneO.setAttribute('width','100'); zoneO.setAttribute('height','14'); zoneO.setAttribute('fill','#f88639');
    svg.insertBefore(zoneO, handR);
    const divH=document.createElementNS('http://www.w3.org/2000/svg','line');
    divH.setAttribute('x1','300'); divH.setAttribute('y1','472'); divH.setAttribute('x2','500'); divH.setAttribute('y2','472'); divH.setAttribute('stroke','#111111'); divH.setAttribute('stroke-width','2');
    svg.insertBefore(divH, handR);
    const divV=document.createElementNS('http://www.w3.org/2000/svg','line');
    divV.setAttribute('x1','400'); divV.setAttribute('y1','472'); divV.setAttribute('x2','400'); divV.setAttribute('y2','486'); divV.setAttribute('stroke','#111111'); divV.setAttribute('stroke-width','2');
    svg.insertBefore(divV, handR);
    const handL=document.createElementNS('http://www.w3.org/2000/svg','image');
    handL.setAttribute('id','lapHandL'); handL.setAttribute('href', window.__resources.doigtGauche); handL.setAttribute('width','42'); handL.setAttribute('height','52');
    handL.setAttribute('transform','translate(329,472)');
    svg.insertBefore(handL, handR);
    const cargo=document.createElementNS('http://www.w3.org/2000/svg','text');
    cargo.setAttribute('id','lapCargo'); cargo.setAttribute('font-size','56'); cargo.textContent='🍄';
    svg.insertBefore(cargo, cursor);
    if(window.lapRaf) cancelAnimationFrame(window.lapRaf);
    const t0=performance.now(); const CYC=11;
    const middle={x:400,y:200}, topRight={x:560,y:100}, bottomLeft={x:235,y:300}, wander={x:300,y:150};
    const ease=p=>{ p=Math.max(0,Math.min(1,p)); return p<.5?2*p*p:1-Math.pow(-2*p+2,2)/2; };
    const mix=(a,b,p)=>{ const e=ease(p); return {x:a.x+(b.x-a.x)*e, y:a.y+(b.y-a.y)*e}; };
    const anim=(now)=>{
      if(current.kind!=='demoB') return;
      const t=((now-t0)/1000)%CYC;
      let c, o, handOn=false;
      if(t<0.6){ c=mix(bottomLeft,middle,t/0.6); o=middle; }
      else if(t<0.95){ c=middle; o=middle; handOn=true; }
      else if(t<2.9){ c=mix(middle,topRight,(t-0.95)/1.95); o=c; handOn=true; }
      else if(t<3.3){ c=topRight; o=topRight; }
      else if(t<4.65){ c=mix(topRight,wander,(t-3.3)/1.35); o=topRight; }
      else if(t<6.0){ c=mix(wander,topRight,(t-4.65)/1.35); o=topRight; }
      else if(t<6.35){ c=topRight; o=topRight; handOn=true; }
      else if(t<8.9){ c=mix(topRight,bottomLeft,(t-6.35)/2.55); o=c; handOn=true; }
      else { c=bottomLeft; o=bottomLeft; }
      let hx=300+(c.x-140)/520*200, hy=440+(c.y-48)/298*32;
      hx=Math.max(305,Math.min(495,hx)); hy=Math.max(442,Math.min(470,hy));
      handL.style.opacity = handOn?'1':'0';
      pressClic(zoneB, '#005da8', handOn);   // le bouton bleu s'eclaire tant qu'il est maintenu enfonce
      handR.setAttribute('transform',`translate(${hx-33},${hy})`);
      cursor.setAttribute('transform',`translate(${c.x-9},${c.y-6})`);
      cargo.setAttribute('x', o.x-20); cargo.setAttribute('y', o.y+34);
      window.lapRaf=requestAnimationFrame(anim);
    };
    window.lapRaf=requestAnimationFrame(anim);
    jouerB(1);
  }

  /* ===== Mission 1 — labyrinthe : flèche à l'entrée, puis papillon confiné aux allées =====
     Détection de collision au pixel près (canvas hors-écran lisant l'image réelle) : les rectangles
     approximatifs rendaient certains couloirs trop étroits pour laisser passer le papillon. */
  const MB_ENTREE={x:41,y:96}, MB_SORTIE={x:41,y:4};
  const MB_ENTREE_X=[34,49]; // colonne d'entrée : reste franchissable même sous le bas du labyrinthe
  const MB_BORD=[16,88]; // limites gauche/droite du labyrinthe (hors de cette plage : hors jeu)
  function bMission1(){
    current={kind:'mission1B'}; majProgB(1,true);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden">
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:6px; margin-bottom:2vh; z-index:30; align-self:center">${CFG.mission1.ecran}</div>
      <div class="mb-laby" id="labyField">
        <div class="mb-fleche" id="labyFleche" style="left:${MB_ENTREE.x}%; top:${MB_ENTREE.y}%">⬆️</div>
        <img class="mb-pap" id="labyPap" src="${window.__resources.papillonB}" alt="papillon" style="left:${MB_ENTREE.x}%; top:${MB_ENTREE.y}%">
      </div>
      <div class="m1-win" id="mb1Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">Mission réussie ✓</div></div></div>
    </div>`;
    const field=document.getElementById('labyField');
    field.style.backgroundImage=`url('${window.__resources.labyrintheB}')`;
    const fleche=document.getElementById('labyFleche'), pap=document.getElementById('labyPap');
    let done=false, actif=false, lastBump=0;
    const setPap=(x,y)=>{ pap.style.left=x+'%'; pap.style.top=y+'%'; };
    // Canvas hors-écran : on lit les pixels réels de labyrinthe-m1-v3.png pour savoir si (x%,y%) tombe sur une haie (opaque).
    const cnv=document.createElement('canvas'); const cctx=cnv.getContext('2d', {willReadFrequently:true});
    let pixelsPrets=false;
    const imgLaby=new Image();
    imgLaby.onload=()=>{ cnv.width=imgLaby.naturalWidth; cnv.height=imgLaby.naturalHeight; cctx.drawImage(imgLaby,0,0); pixelsPrets=true; };
    imgLaby.src=window.__resources.labyrintheB;
    const RAYON_PAP=1.0; // % du champ : petite marge de tolérance autour du curseur (certains passages du labyrinthe sont naturellement étroits)
    const surHaie=(xPct,yPct)=>{
      if(!pixelsPrets) return false;
      const rx=RAYON_PAP/100*cnv.width, ry=RAYON_PAP/100*cnv.height;
      const cx=xPct/100*cnv.width, cy=yPct/100*cnv.height;
      const x0=Math.max(0,Math.round(cx-rx)), y0=Math.max(0,Math.round(cy-ry));
      const x1=Math.min(cnv.width,Math.round(cx+rx)), y1=Math.min(cnv.height,Math.round(cy+ry));
      const bw=x1-x0, bh=y1-y0;
      if(bw<=0||bh<=0) return false;
      const data=cctx.getImageData(x0,y0,bw,bh).data;
      for(let i=3;i<data.length;i+=4){ if(data[i]>80) return true; }
      return false;
    };
    const horsLabyrinthe=(xPct,yPct)=>{
      if(xPct<MB_BORD[0] || xPct>MB_BORD[1]) return true;
      if(yPct>90.5 && !(xPct>=MB_ENTREE_X[0] && xPct<=MB_ENTREE_X[1])) return true;
      return false;
    };
    const heurte=(x,y)=> surHaie(x,y) || horsLabyrinthe(x,y);
    // La flèche marque l'entrée ; après un court instant elle disparaît et le papillon (attrapable) prend sa place.
    bTempo(1300, ()=>{ if(current.kind!=='mission1B') return; fleche.classList.add('hide'); pap.classList.add('show'); actif=true; });
    pap.addEventListener('pointerdown', e=>{ if(!actif||done)return; e.preventDefault(); try{pap.setPointerCapture(e.pointerId);}catch(_){}; pap.classList.add('tenu'); pap.dataset.tenu='1'; });
    pap.addEventListener('pointermove', e=>{
      if(!pap.dataset.tenu||done)return;
      const r=field.getBoundingClientRect();
      const nx=Math.max(1,Math.min(99,(e.clientX-r.left)/r.width*100));
      const ny=Math.max(1,Math.min(99,(e.clientY-r.top)/r.height*100));
      if(heurte(nx,ny)){
        // Collision : le papillon est relâché à la position valide la plus proche (celle déjà atteinte) — on ne repart pas du début.
        const now=performance.now(); if(now-lastBump>400){ lastBump=now; secoueEcran(); }
        pap.dataset.tenu=''; pap.classList.remove('tenu');
        return;
      }
      setPap(nx,ny);
      if(!done && Math.hypot(nx-MB_SORTIE.x, ny-MB_SORTIE.y)<7){
        done=true; pap.dataset.tenu=''; pap.classList.remove('tenu');
        majProgB(2,true); if(typeof bipOk==='function') bipOk();
        const w=document.getElementById('mb1Win'); if(w) w.classList.add('show');
        jouerVictoireB(0, bSuite);
      }
    });
    const relacher=()=>{ pap.dataset.tenu=''; pap.classList.remove('tenu'); };
    pap.addEventListener('pointerup', relacher); pap.addEventListener('pointercancel', relacher);
    jouerB(2);
  }

  /* ===== Mission 2 — fiole : maintien du clic pour la remplir (vitesse réduite) ===== */
  function bMission2(){
    current={kind:'mission2B'}; majProgB(2,true);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden">
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; margin-bottom:2vh; z-index:30">${CFG.mission2.ecran}</div>
      <div class="mb-fiole-wrap">
        <div class="mb-fiole" id="mbFiole">
          <img class="mb-fiole-img mb-fiole-vide" src="${window.__resources.fioleB}" alt="">
          <div class="mb-fiole-plein" id="mbJauge"><img class="mb-fiole-img" src="${window.__resources.fioleB}" alt="fiole Bois-moi"></div>
        </div>
        <div class="mb-fiole-pct" id="mbPct" style="color:hsl(0,70%,45%)">0%</div>
      </div>
      <div class="m1-win" id="mb2Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">Mission réussie ✓</div></div></div>
    </div>`;
    const fiole=document.getElementById('mbFiole'), jauge=document.getElementById('mbJauge'), pctEl=document.getElementById('mbPct');
    let pct=0, done=false;
    const loop=()=>{
      if(done||current.kind!=='mission2B')return;
      pct=Math.max(0, Math.min(100, pct+(fiole.dataset.tenu?0.22:-0.18)));
      jauge.style.clipPath='inset('+(100-pct)+'% 0 0 0)';
      pctEl.textContent=Math.round(pct)+'%'; pctEl.style.color='hsl('+(pct*1.2)+',70%,45%)';
      if(pct>=100){
        done=true; majProgB(3,true); if(typeof bipOk==='function') bipOk();
        const w=document.getElementById('mb2Win'); if(w) w.classList.add('show');
        jouerVictoireB(1, bSuite);
        return;
      }
      bRaf=requestAnimationFrame(loop);
    };
    fiole.addEventListener('pointerdown', e=>{ if(done)return; e.preventDefault(); try{fiole.setPointerCapture(e.pointerId);}catch(_){}; fiole.dataset.tenu='1'; fiole.classList.add('tenu'); bipClic(); });
    const relacher=()=>{ fiole.dataset.tenu=''; fiole.classList.remove('tenu'); };
    fiole.addEventListener('pointerup', relacher); fiole.addEventListener('pointercancel', relacher);
    bRaf=requestAnimationFrame(loop);
    jouerB(3);
  }

  /* ===== Mission 3 — présentoir : aperçu complet, puis ombres à remplir + guirlande façon horloge ===== */
  const MB_ITEMS=[
    {key:'itemCupcake', tx:50, ty:17, sx:16, sy:50},
    {key:'itemTheiere', tx:21, ty:32, sx:31, sy:50},
    {key:'itemMontre',  tx:79, ty:32, sx:46, sy:50},
    {key:'itemCle',     tx:21, ty:68, sx:61, sy:50},
    {key:'itemGateau',  tx:79, ty:68, sx:76, sy:50},
    {key:'itemTasse',   tx:50, ty:83, sx:91, sy:50}
  ];
  function bMission3(){
    current={kind:'mission3B'}; majProgB(3,true);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden; align-items:center">
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; margin-bottom:2vh; z-index:30">${CFG.mission3.ecran}</div>
      <div class="mb-table" id="mbTable"><div class="mb-table-fil" id="mbFil"></div></div>
      <div class="mb-stock" id="mbStock"></div>
      ${MB_ITEMS.map((it,i)=>`<div class="mb-objet" id="mbObj${i}"><img src="${window.__resources[it.key]}" alt=""></div>`).join('')}
      <div class="m1-win" id="mb3Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">Mission réussie ✓</div></div></div>
    </div>`;
    const table=document.getElementById('mbTable'), fil=document.getElementById('mbFil'), stock=document.getElementById('mbStock');
    table.style.backgroundImage=`url('${window.__resources.presentoirVide}')`;
    fil.style.backgroundImage=`url('${window.__resources.presentoirComplet}')`;
    fil.style.setProperty('--p','1'); // aperçu initial : guirlande + items déjà en place, rien n'est décollé
    const placer=(el,rect,px,py)=>{ el.style.left=(rect.left+px/100*rect.width)+'px'; el.style.top=(rect.top+py/100*rect.height)+'px'; };
    MB_ITEMS.forEach((it,i)=>{ placer(document.getElementById('mbObj'+i), table.getBoundingClientRect(), it.tx, it.ty); });
    let actif=false, ranges=0, done=false, held=null;
    const majGuirlande=()=>{ fil.style.setProperty('--p', String(ranges/MB_ITEMS.length)); };
    bTempo(1800, ()=>{
      if(current.kind!=='mission3B')return;
      fil.style.setProperty('--p','0'); // fin de l'aperçu : la table repasse en mode « ombres à remplir »
      const sr=stock.getBoundingClientRect();
      MB_ITEMS.forEach((it,i)=>{ const el=document.getElementById('mbObj'+i); el.classList.add('animating'); placer(el, sr, it.sx, it.sy); });
      setTimeout(()=>{ if(current.kind==='mission3B') MB_ITEMS.forEach((it,i)=>{ document.getElementById('mbObj'+i).classList.remove('animating'); }); }, 700);
      actif=true;
    });
    MB_ITEMS.forEach((it,i)=>{
      const el=document.getElementById('mbObj'+i);
      el.addEventListener('pointerdown', e=>{ if(!actif||done||el.dataset.ok)return; e.preventDefault(); held=i; el.classList.add('tenu'); try{el.setPointerCapture(e.pointerId);}catch(_){}; bipClic(); });
      el.addEventListener('pointermove', e=>{ if(held!==i)return; el.style.left=e.clientX+'px'; el.style.top=e.clientY+'px'; });
      const lacher=e=>{
        if(held!==i)return; held=null; el.classList.remove('tenu');
        const tr=table.getBoundingClientRect();
        const cx=tr.left+it.tx/100*tr.width, cy=tr.top+it.ty/100*tr.height;
        const d=Math.hypot((e.clientX||cx)-cx, (e.clientY||cy)-cy);
        if(d<Math.min(tr.width,tr.height)*0.11){
          // L'item remplace uniquement sa propre ombre : les autres ombres restent inchangées.
          el.style.left=cx+'px'; el.style.top=cy+'px'; el.dataset.ok='1'; el.style.pointerEvents='none'; el.classList.add('pose');
          ranges++; majGuirlande(); bip(523,0.1);
          if(ranges>=MB_ITEMS.length){
            done=true; majProgB(4); if(typeof bipOk==='function') bipOk();
            table.style.backgroundImage=`url('${window.__resources.presentoirFinal}')`; fil.style.display='none';
            const w=document.getElementById('mb3Win'); setTimeout(()=>{ if(w) w.classList.add('show'); }, 500);
            jouerVictoireB(2, bSuite);
          }
        } else {
          el.classList.add('animating');
          const sr=stock.getBoundingClientRect(); placer(el, sr, it.sx, it.sy);
          setTimeout(()=>el.classList.remove('animating'), 650);
        }
      };
      el.addEventListener('pointerup', lacher); el.addEventListener('pointercancel', lacher);
    });
    jouerB(4);
  }

  /* ===== Badge de fin (même traitement que le niveau A) ===== */
  function bBadge(){
    current={kind:'badgeB'}; majProgB(4);
    setHeaderTools(false);
    const confettis=Array.from({length:14}).map((_,k)=>{ const left=6+k*6.5, delay=(k%7)*0.32, emo=['🎉','✨','⭐','🎊','🌟'][k%5]; return `<div class="conf" style="left:${left}%;animation-delay:${delay}s">${emo}</div>`; }).join('');
    mainEl().innerHTML=`<div class="screen active">
      <div class="victoire">
        ${confettis}
        <div class="medaille-img" style="font-size:clamp(70px,12vw,130px)">🏅</div>
        <h1 id="titre">Bravo !</h1>
        <div class="kompa-bulle sans-queue" style="position:static; white-space:pre-line; max-width:min(640px,64vw)">${CFG.badge.ecran}</div>
      </div>
    </div>`;
    bipOk(); try{ pqSetFait('B'); }catch(_){}
    jouerB(5);
  }

  const bParcours=[ bPresentation, bDemo, bMission1, bMission2, bMission3, bBadge ];
  function majRetourB(){ if(typeof retourBtn!=='undefined' && retourBtn){ retourBtn.disabled=(bEtape===0); } }
  function majNavB(){
    const nav=document.getElementById('nbNav'); if(!nav) return;
    const prec=document.getElementById('nbPrecBtn'), suiv=document.getElementById('nbSuivBtn');
    if(prec) prec.disabled=(bEtape===0);
    if(suiv) suiv.style.display=(bEtape===bParcours.length-1)?'none':'';
  }
  function bAllerA(i){
    bEtape=Math.max(0,Math.min(bParcours.length-1,i)); clearB();
    try{ if(window.speechSynthesis) speechSynthesis.cancel(); }catch(_){}
    bParcours[bEtape](); majRetourB(); majNavB();
  }

  function lancer(){
    debloqueAudio(); musiqueStart(); musiqueDuck(true);
    if(typeof pqEl!=='undefined' && pqEl) pqEl.classList.remove('on');
    setHeaderTools(true);
    if(typeof retourBtn!=='undefined' && retourBtn) retourBtn.onclick=()=>bAllerA(bEtape-1);
    const nav=document.getElementById('nbNav'), prec=document.getElementById('nbPrecBtn'), suiv=document.getElementById('nbSuivBtn');
    if(nav){ nav.style.display='flex'; if(prec) prec.onclick=()=>{ bipClic(); bAllerA(bEtape-1); }; if(suiv) suiv.onclick=()=>{ bipClic(); bAllerA(bEtape+1); }; }
    bipClic(); bEtape=0; bAllerA(0);
  }
  function stop(){
    clearB(); try{ if(window.speechSynthesis) speechSynthesis.cancel(); }catch(_){}
    if(typeof retourBtn!=='undefined' && retourBtn) retourBtn.onclick=()=>allerA(etape-1);
    const nav=document.getElementById('nbNav'); if(nav) nav.style.display='none';
    const n4=document.querySelector('#steps .snode[data-i="4"]'); if(n4) n4.style.display='';
  }
  return { lancer:lancer, stop:stop };
})();
window.NB = NB;
function lancerNiveauB(){ if(window.NB) window.NB.lancer(); }
/* =================== FIN NIVEAU B =================== */

/* ===================== NIVEAU C (intégré, module isolé) — v3 (retours intégrés) ===================== */
const NC = (function(){
  const CFG = {
    titreNotion:"Le clic droit et le défilement à deux doigts",
    presentation:{
      oral:"Re-bonjour les Kopains, c'est le Professeur Pile Poil ! Vous vous souvenez, je vous avais dit que le clic droit aurait son heure de gloire plus tard ? Eh bien... la voici ! Un clic droit ouvre un petit menu avec plusieurs choix écrits. À vous de cliquer sur le bon avec un clic gauche, comme d'habitude. Le clic gauche choisit, le clic droit propose, les deux travaillent ensemble !",
      ecran:"Un clic droit ouvre un menu.\nUn clic gauche permet ensuite de choisir dedans."
    },
    phaseTest:{
      oral:"Pour s'échauffer, voici un petit coffre à bonbons. Posez le curseur dessus et faites un clic droit : un menu va s'ouvrir avec plusieurs choses écrites dedans. Choisissez l'option « Ouvrir » avec un clic gauche, et régalez-vous !",
      ecran:"Fais un clic droit, puis choisis l'option « Ouvrir » avec un clic gauche.",
      reussi:"Miam, plein de bonbons ! Vous savez ouvrir un menu et choisir dedans, en avant pour la suite !",
      ratéFermer:"Oh, le coffre reste fermé... Refais un clic droit et choisis cette fois l'option « Ouvrir » !",
      ratéSupprimer:"Non, il ne faut surtout pas supprimer le coffre à bonbons ! Refais un clic droit et choisis l'option « Ouvrir »."
    },
    mission1:{
      titre:"Le menu du Lapin Blanc",
      consigne:"Regarde, le Lapin Blanc traverse le jardin en courant, il a l'air terriblement pressé ! Vite, pose le curseur sur lui avant qu'il ne disparaisse, et fais un clic droit : un menu avec trois mots va s'ouvrir. Choisis l'option « Suivre le Lapin » avec un clic gauche pour te lancer à sa poursuite dans le terrier !",
      ecran:"Fais un clic droit sur le Lapin, puis choisis l'option « Suivre le Lapin ».",
      reussi:"Et hop, vous voilà sur les talons du Lapin, direction le terrier ! Vous avez ouvert le menu et fait le bon choix, bravo !",
      raté:"Hmm, ce n'était pas ça... le Lapin est toujours pressé, refais un clic droit sur lui et choisis bien l'option « Suivre le Lapin »."
    },
    mission2:{
      titre:"Le tri des cartes-soldats",
      consigne:"Toute la garde de la Reine de Cœur est alignée devant toi, et chaque carte-soldat attend ses ordres ! Fais un clic droit sur chacune, regarde bien sa posture, et choisis l'option « Garde » ou l'option « Repos » selon ce que tu vois. Il faut toutes les traiter pour que la Reine soit satisfaite !",
      ecran:"Fais un clic droit sur chaque carte, puis choisis l'option qui correspond.",
      reussi:"Parfait, chaque carte a reçu le bon ordre, la garde de la Reine est fière de vous !"
    },
    presentationScroll:{
      oral:"Bravo les Kompagnons ! Il vous reste un dernier geste à apprendre : le défilement à deux doigts. Posez deux doigts côte à côte sur le trackpad et glissez-les ensemble vers le bas : le décor défile comme un long parchemin qui se déroule ! Glissez-les vers le haut pour remonter.",
      ecran:"Pose deux doigts sur le trackpad et glisse-les pour faire défiler l'écran."
    },
    mission3a:{
      titre:"Le couloir sans fin du Chat du Cheshire",
      consigne:"En suivant le Lapin, tu es tombé dans un looong couloir qui n'en finit pas... et le Chat du Cheshire adore justement s'y cacher ! Fais défiler avec deux doigts jusqu'à l'apercevoir, et clique dessus, prends ton temps, il ne bougera pas !",
      ecran:"Fais défiler avec deux doigts et clique sur le Chat quand tu le vois.",
      reussi:"Vous l'avez trouvé ! Le Chat vous indique la sortie du couloir, en avant vers le jardin !"
    },
    mission4:{
      titre:"Le coffre-fort final",
      consigne:"En sortant du couloir, tu débouches dans le grand jardin du Pays des Merveilles, et un vrai trésor s'y cache quelque part ! Fais défiler avec deux doigts pour explorer. Ramasse les pièces qui traînent, ouvre les coffres que tu croises, et pour chaque buisson : clic droit, puis choisis bien l'option « Ouvrir » ou l'option « Supprimer », attention, un buisson, ça ne s'ouvre pas !",
      ecran:"Fais défiler le jardin, ramasse les pièces, et clic droit sur les coffres et les buissons.",
      reussi:"INCROYABLE ! Vous avez trouvé le trésor et vous êtes de vrais champions du trackpad !",
      ratéBuisson:"Un buisson, ça ne s'ouvre pas ! Regarde bien avant de choisir : « Ouvrir » ou « Supprimer » ?",
      ratéCoffre:"Ce n'est pas un buisson, celui-là, c'est un coffre ! Choisis plutôt l'option « Ouvrir ».",
      ratéCoffreFinal:"Presque ! Ce trésor-là, il faut l'« Ouvrir », pas le supprimer !"
    },
    badge:{
      oral:"Et... badge débloqué ! Vous maîtrisez maintenant le clic droit pour ouvrir des menus, et le défilement à deux doigts pour explorer un décor tout entier. Le vrai trésor est trouvé, et avec lui... le Permis du Trackpad tout entier ! Vous savez déplacer, cliquer, maintenir-glisser, faire un clic droit et faire défiler à deux doigts. Bravo à tous les Kompagnons, vous êtes prêts pour le premier atelier de codage !",
      ecran:"Badge débloqué : clic droit et défilement à deux doigts maîtrisés !\n\n🏅 Permis du Trackpad obtenu, prochaine étape : le premier atelier de codage !"
    }
  };

  /* Voix off (mp3) — squelette en attente des fichiers réels. */
  const NC_AUDIO=[null,null,null,null,null,null,null,null];
  const NC_VICTOIRE=[null,null,null,null];
  const ncAudioEl=new Audio(); ncAudioEl.preload='auto';
  const ncVicEl=new Audio(); ncVicEl.preload='auto';
  function jouerC(i, onEnd){
    const src=NC_AUDIO[i];
    // Tant qu'aucun audio n'est fourni, on NE declenche PAS l'enchainement : la nav Prec./Suiv. reste la main.
    if(!src||!sonOn) return;
    try{
      ncAudioEl.pause(); ncAudioEl.currentTime=0; ncAudioEl.onended=null;
      ncAudioEl.src=src;
      if(onEnd) ncAudioEl.onended=()=>onEnd();
      const p=ncAudioEl.play();
      if(p&&p.catch) p.catch(()=>{ if(onEnd) cTempo(1200, onEnd); });
    }catch(e){ if(onEnd) cTempo(1200, onEnd); }
  }
  function jouerVictoireC(i, onEnd){
    const src=NC_VICTOIRE[i];
    if(!src||!sonOn) return;
    try{
      ncVicEl.pause(); ncVicEl.currentTime=0; ncVicEl.onended=null;
      ncVicEl.src=src;
      if(onEnd) ncVicEl.onended=()=>onEnd();
      const p=ncVicEl.play();
      if(p&&p.catch) p.catch(()=>{ if(onEnd) cTempo(1200, onEnd); });
    }catch(e){ if(onEnd) cTempo(1200, onEnd); }
  }
  /* Les voix off ne sont pas encore fournies : tant que NC_AUDIO est vide, la nav Prec./Suiv. reste
     affichee et pilote le passage d'une slide a l'autre. Des qu'un audio est renseigne, l'enchainement
     devient automatique (comme au niveau A) et la nav disparait. */
  function cAudioPret(){ return NC_AUDIO.some(Boolean); }
  function cSuite(){ if(cAudioPret()) allerCA(cEtape+1); }

  let cEtape=0, cRaf=null, cTempoId=null, cTempoId2=null, ncOverlayResize=null;
  function mainEl(){ return document.getElementById('main'); }
  function clearC(){
    if(cRaf){cancelAnimationFrame(cRaf);cRaf=null;} if(cTempoId){clearTimeout(cTempoId);cTempoId=null;} if(cTempoId2){clearTimeout(cTempoId2);cTempoId2=null;}
    if(window.lapRaf){cancelAnimationFrame(window.lapRaf); window.lapRaf=null;} fermerMenuC();
    if(ncOverlayResize){ window.removeEventListener('resize', ncOverlayResize); ncOverlayResize=null; }
    const vid=document.getElementById('mcScrollVideo'); if(vid){ try{ vid.pause(); }catch(_){} }
    ['__ncPresIv','__ncScrollIv','__ncDemoChain'].forEach(k=>{ if(window[k]){ clearInterval(window[k]); clearTimeout(window[k]); window[k]=null; } });
  }
  function cTempo(ms,fn){ if(cTempoId)clearTimeout(cTempoId); cTempoId=setTimeout(fn,ms); }
  function majProgC(n, active){
    setProgress(n, active);
    const l=document.getElementById('progLabel'); if(l){ l.textContent='NIVEAU C'; l.style.color='#4A4A4A'; }
    // Le niveau C compte 4 missions (comme le niveau A) : les 4 pastilles restent visibles.
    const n4=document.querySelector('#steps .snode[data-i="4"]'); if(n4) n4.style.display='';
  }

  /* ===== Décor de scène : remplace le damier par un fond doux + quelques assets existants dispersés ===== */
  const DECORS = {
    bonbons:[
      {src:()=>window.__resources.lantRoseOn, style:'left:6%; top:14%; width:80px; transform:rotate(-8deg)'},
      {src:()=>window.__resources.lantOrangeOn, style:'right:8%; top:10%; width:70px; transform:rotate(6deg)'},
      {src:()=>window.__resources.papillonLow, style:'left:10%; bottom:12%; width:60px'},
      {src:()=>window.__resources.itemCupcake, style:'right:10%; bottom:16%; width:70px'}
    ],
    lapin:[
      {src:()=>window.__resources.porteFlamantImg, style:'left:4%; top:8%; width:110px'},
      {src:()=>window.__resources.lantVertOn, style:'right:6%; top:16%; width:70px'},
      {src:()=>window.__resources.papillonLow, style:'right:12%; bottom:10%; width:56px'},
      {src:()=>window.__resources.itemMontre, style:'left:9%; bottom:14%; width:60px'}
    ],
    cartes:[
      {src:()=>window.__resources.porteChapelierImg, style:'left:4%; top:6%; width:120px'},
      {src:()=>window.__resources.lantBleuOn, style:'right:6%; top:12%; width:70px'},
      {src:()=>window.__resources.lantRoseOn, style:'left:8%; bottom:10%; width:64px'},
      {src:()=>window.__resources.itemTasse, style:'right:10%; bottom:14%; width:60px'}
    ]
  };
  function decorScene(kind){
    const items=DECORS[kind]||[];
    const imgs=items.map(it=>`<img class="mc-decor-item" src="${it.src()}" alt="" style="${it.style}">`).join('');
    return `<div class="mc-decor-scene">${imgs}</div>`;
  }

  /* ===== Menu contextuel simulé : clic droit -> options (icône image OU emoji + texte) -> clic gauche pour choisir ===== */
  let menuActif=null;
  function fermerMenuC(){ if(menuActif){ menuActif.remove(); menuActif=null; } document.removeEventListener('click', fermerMenuAuClicExterieur, true); }
  function fermerMenuAuClicExterieur(e){ if(menuActif && !menuActif.contains(e.target)){ fermerMenuC(); } }
  function ouvrirMenuC(x, y, options, onChoisir){
    fermerMenuC();
    const menu=document.createElement('div'); menu.className='mc-menu';
    const vx=Math.min(x, window.innerWidth-210), vy=Math.min(y, window.innerHeight-16-options.length*46);
    menu.style.left=vx+'px'; menu.style.top=vy+'px';
    options.forEach(opt=>{
      const b=document.createElement('button'); b.type='button';
      const ic = opt.img ? `<img class="mc-menu-ic" src="${opt.img}" alt="">` : (opt.emoji ? `<span class="mc-menu-ic" style="font-size:20px; line-height:1">${opt.emoji}</span>` : '');
      b.innerHTML = ic + `<span>${opt.label}</span>`;
      b.addEventListener('click', ev=>{ ev.stopPropagation(); fermerMenuC(); onChoisir(opt.id); });
      menu.appendChild(b);
    });
    document.body.appendChild(menu); menuActif=menu;
    setTimeout(()=>document.addEventListener('click', fermerMenuAuClicExterieur, true), 0);
  }
  /* petit texte flottant de gain (pièces d'or) */
  function gainVole(texte, x, y){
    const g=document.createElement('div'); g.className='mc-gain-vole'; g.textContent=texte;
    g.style.left=x+'px'; g.style.top=y+'px'; document.body.appendChild(g);
    setTimeout(()=>g.remove(), 1050);
  }

  /* ===== Calage précis de l'overlay HTML sur l'écran dessiné du laptop SVG =====
     L'écran occupe le rectangle x=140,y=48,w=520,h=298 dans le viewBox 800x720 du SVG.
     On recalcule sa position/taille réelles en pixels (au lieu de % fixes sur .lap-wrap,
     qui ne correspondent pas toujours au rendu réel du SVG selon la taille de l'écran)
     pour que la fenêtre/vidéo Scratch restent toujours bien à l'intérieur de l'écran du laptop. */
  const ECRAN_VB={ x:140, y:48, w:520, h:298 };
  function ajusterOverlayEcran(lapWrap, svg, overlay){
    const wrapRect=lapWrap.getBoundingClientRect(), svgRect=svg.getBoundingClientRect();
    const echelle=svgRect.width/800;
    overlay.style.left=((svgRect.left-wrapRect.left)+ECRAN_VB.x*echelle)+'px';
    overlay.style.top=((svgRect.top-wrapRect.top)+ECRAN_VB.y*echelle)+'px';
    overlay.style.width=(ECRAN_VB.w*echelle)+'px';
    overlay.style.height=(ECRAN_VB.h*echelle)+'px';
  }

  /* ===== Démo "ordinateur" réutilisable : main qui clique-droit OU deux doigts qui défilent, sur un vrai laptop ===== */
  function demoOrdinateur(mode){ // mode: 'clicDroit' | 'deuxDoigts'
    const svg=mainEl().querySelector('.lap-svg');
    const hand=svg.querySelector('#lapHand'), cursor=svg.querySelector('#lapCursor');
    if(window.lapRaf) cancelAnimationFrame(window.lapRaf);
    const lapWrap=mainEl().querySelector('.lap-wrap'); lapWrap.style.position='relative';
    // overlay HTML calé précisément sur l'écran du laptop (zone blanche du SVG), recalculé au redimensionnement
    const overlay=document.createElement('div'); overlay.className='mc-lap-screen-overlay';
    lapWrap.appendChild(overlay);
    ajusterOverlayEcran(lapWrap, svg, overlay);
    if(ncOverlayResize) window.removeEventListener('resize', ncOverlayResize);
    ncOverlayResize=()=>ajusterOverlayEcran(lapWrap, svg, overlay);
    window.addEventListener('resize', ncOverlayResize);

    const menuHTML=`<div class="mc-demo-menu" id="mcDemoMenu" style="left:60%; top:54%; z-index:3"><div>🍬 Ouvrir</div><div>📋 Copier</div><div>📄 Coller</div></div>`;
    overlay.insertAdjacentHTML('beforeend', menuHTML);
    const demoMenu=document.getElementById('mcDemoMenu');

    if(mode==='clicDroit'){
      hand.setAttribute('href', window.__resources.doigtDroit); hand.setAttribute('width','66'); hand.setAttribute('height','82');
      hand.setAttribute('transform','translate(400,440)');
      // Zone bleue (clic gauche, pour choisir dans le menu) + zone orange (clic droit, pour ouvrir le menu) — même code couleur que le Niveau B
      const zoneB=document.createElementNS('http://www.w3.org/2000/svg','rect');
      zoneB.setAttribute('x','300'); zoneB.setAttribute('y','472'); zoneB.setAttribute('width','100'); zoneB.setAttribute('height','14'); zoneB.setAttribute('fill','#005da8');
      svg.insertBefore(zoneB, hand);
      const zoneO=document.createElementNS('http://www.w3.org/2000/svg','rect');
      zoneO.setAttribute('x','400'); zoneO.setAttribute('y','472'); zoneO.setAttribute('width','100'); zoneO.setAttribute('height','14'); zoneO.setAttribute('fill','#f88639');
      svg.insertBefore(zoneO, hand);
      const divH=document.createElementNS('http://www.w3.org/2000/svg','line');
      divH.setAttribute('x1','300'); divH.setAttribute('y1','472'); divH.setAttribute('x2','500'); divH.setAttribute('y2','472'); divH.setAttribute('stroke','#111111'); divH.setAttribute('stroke-width','2');
      svg.insertBefore(divH, hand);
      const divV=document.createElementNS('http://www.w3.org/2000/svg','line');
      divV.setAttribute('x1','400'); divV.setAttribute('y1','472'); divV.setAttribute('x2','400'); divV.setAttribute('y2','486'); divV.setAttribute('stroke','#111111'); divV.setAttribute('stroke-width','2');
      svg.insertBefore(divV, hand);
      // Bureau Windows (avec l'icône Scratch déjà dessus, S bien visible) posé sur l'écran + fenêtre (cachée au départ, légèrement réduite pour bien rester dans l'écran)
      overlay.insertAdjacentHTML('beforeend', `
        <img id="mcAppIcon" src="${window.__resources.bureauScratchImg}" alt="Bureau avec l'icône Scratch" style="position:absolute; inset:0; width:100%; height:100%; object-fit:fill; transition:opacity .3s ease; z-index:1">
        <img id="mcAppWindow" src="${window.__resources.scratchFenetreImg}" alt="Fenêtre Scratch" style="position:absolute; left:2%; top:2%; width:96%; height:96%; object-fit:fill; opacity:0; transition:opacity .4s ease; z-index:2">
      `);
      demoMenu.style.left='21%'; demoMenu.style.top='3%';
      const icon=document.getElementById('mcAppIcon'), fenetre=document.getElementById('mcAppWindow');
      // Le curseur du SVG serait masqué par l'icône/la fenêtre HTML posées par-dessus : on le remplace ici
      // par un curseur HTML, ajouté en dernier dans l'overlay pour rester visible au-dessus de tout.
      cursor.style.opacity='0';
      const curseurHTML=document.createElement('img'); curseurHTML.src=CURSOR_SRC; curseurHTML.alt='';
      curseurHTML.style.cssText='position:absolute; width:4%; height:auto; pointer-events:none; z-index:4;';
      overlay.appendChild(curseurHTML);
      // La pointe de la flèche est en haut à gauche de l'image : on décale pour qu'elle tombe pile sur la cible.
      const placerCurseur=(xPct,yPct)=>{ curseurHTML.style.left=(xPct-0.9)+'%'; curseurHTML.style.top=(yPct-1.2)+'%'; };
      // Position (en % de l'écran) de l'icône Scratch telle que dessinée dans assets/images/bureauScratchImg.png
      const posIcone=()=>({ x:13.99, y:7.25 });
      // Position (en % de l'écran) du centre d'une des trois options du menu
      const posOption=(i)=>{
        const o=overlay.getBoundingClientRect(), r=demoMenu.children[i].getBoundingClientRect();
        return { x:((r.left-o.left)+r.width*0.30)/o.width*100, y:((r.top-o.top)+r.height/2)/o.height*100 };
      };
      // Surlignage de l'option survolée : montre visuellement qu'on a le choix entre les trois
      const surligner=(i)=>{
        Array.from(demoMenu.children).forEach((el,k)=>{
          el.style.background = (k===i) ? 'rgba(248,134,57,.28)' : 'transparent';
          el.style.borderRadius='5px';
        });
      };
      const repos={x:6,y:58};
      // Fait glisser le doigt droit sur la partie BLANCHE du trackpad (au-dessus des zones colorées), en suivant
      // proportionnellement la position du curseur à l'écran — même principe de correspondance que le Niveau B.
      const doigtSurTrackpad=(pct)=>{
        const hx=Math.max(300,Math.min(430, 300+pct.x*2));
        const hy=Math.max(442,Math.min(468, 440+pct.y*0.32));
        hand.setAttribute('transform', `translate(${hx},${hy})`);
      };
      /* Déplace le curseur (et le doigt, qui est ce qui le fait bouger) d'un point à un autre.
         `surlignage` = index de l'option à mettre en évidence DÈS l'arrivée du curseur (-1 = aucune),
         puis on marque une pause avant d'enchaîner, pour laisser le temps de voir. */
      let posCourante={...repos};
      const deplacer=(vers, duree, pause, apres, surlignage)=>{
        const de={...posCourante}, t0=performance.now();
        const pas=(now)=>{
          if(current.kind!=='presentationC') return;
          const p=Math.min(1,(now-t0)/duree), e=p<.5?2*p*p:1-Math.pow(-2*p+2,2)/2;
          const px=de.x+(vers.x-de.x)*e, py=de.y+(vers.y-de.y)*e;
          placerCurseur(px,py); doigtSurTrackpad({x:px,y:py});
          if(p<1){ window.lapRaf=requestAnimationFrame(pas); }
          else {
            posCourante={...vers};
            if(surlignage!==undefined) surligner(surlignage);
            if(apres) window.__ncDemoChain=setTimeout(apres, pause);
          }
        };
        window.lapRaf=requestAnimationFrame(pas);
      };
      placerCurseur(repos.x, repos.y); doigtSurTrackpad(repos);
      const sequence=()=>{
        if(current.kind!=='presentationC') return;
        // 1) retour à l'état de repos (doigt droit, prêt à glisser) — on restaure son gabarit
        hand.setAttribute('href', window.__resources.doigtDroit);
        hand.setAttribute('width','66'); hand.setAttribute('height','82');
        zoneB.setAttribute('fill','#005da8'); zoneO.setAttribute('fill','#f88639');
        demoMenu.classList.remove('show'); surligner(-1);
        fenetre.style.opacity='0'; icon.style.opacity='1';
        posCourante={...repos}; placerCurseur(repos.x, repos.y); doigtSurTrackpad(repos);
        window.__ncDemoChain=setTimeout(()=>{
          if(current.kind!=='presentationC') return;
          // 2) le curseur ET le doigt glissent ensemble jusqu'à l'icône Scratch
          deplacer(posIcone(), 1100, 900, ()=>{
            // 3) le doigt droit descend sur le bouton droit (zone orange) : clic droit -> le menu s'ouvre
            hand.setAttribute('transform','translate(417,472)'); flashClic(zoneO,'#f88639');
            if(typeof bipClic==='function') bipClic();
            window.__ncDemoChain=setTimeout(()=>{
              if(current.kind!=='presentationC') return;
              demoMenu.classList.add('show');
              // 4) on laisse le menu s'afficher, puis le curseur passe sur les TROIS options pour montrer le choix
              window.__ncDemoChain=setTimeout(()=>{
                if(current.kind!=='presentationC') return;
                deplacer(posOption(0), 900, 800, ()=>{
                  deplacer(posOption(1), 700, 800, ()=>{
                    deplacer(posOption(2), 700, 800, ()=>{
                      // 5) retour sur « Ouvrir », le bon choix
                      deplacer(posOption(0), 800, 900, ()=>{
                        if(current.kind!=='presentationC') return;
                        // 6) le doigt gauche vient cliquer sur le bouton gauche (zone bleue) pour valider « Ouvrir ».
                        //    Meme gabarit et meme calage qu'au niveau B, sinon le doigt reste a cote de la zone.
                        hand.setAttribute('href', window.__resources.doigtGauche);
                        hand.setAttribute('width','42'); hand.setAttribute('height','52');
                        hand.setAttribute('transform','translate(329,455)');
                        window.__ncDemoChain=setTimeout(()=>{
                          if(current.kind!=='presentationC') return;
                          hand.setAttribute('transform','translate(329,472)'); flashClic(zoneB,'#005da8');
                          if(typeof bipClic==='function') bipClic();
                          window.__ncDemoChain=setTimeout(()=>{
                            if(current.kind!=='presentationC') return;
                            // 7) « Ouvrir » choisi : le menu se ferme, la fenêtre Scratch s'ouvre
                            demoMenu.classList.remove('show'); surligner(-1);
                            icon.style.opacity='0'; fenetre.style.opacity='1'; if(typeof bipOk==='function') bipOk();
                            window.__ncDemoChain=setTimeout(sequence, 3200);
                          }, 400);
                        }, 700);
                      }, 0);
                    }, 2);
                  }, 1);
                }, 0);
              }, 900);
            }, 300);
          });
        }, 900);
      };
      sequence();
    } else {
      hand.setAttribute('href', window.__resources.deuxDoigtsScrollImg); hand.setAttribute('width','60'); hand.setAttribute('height','75');
      demoMenu.remove();
      // La vidéo Scratch illustre ici un vrai exemple de défilement (dans les blocs / la fenêtre de code), légèrement réduite pour bien rester dans l'écran du laptop
      overlay.insertAdjacentHTML('beforeend', `<video id="mcScrollVideo" src="${window.__resources.scratchDemoVideo}" muted loop autoplay playsinline preload="auto" style="position:absolute; left:2%; top:2%; width:96%; height:96%; object-fit:fill"></video>`);
      const vid=document.getElementById('mcScrollVideo');
      try{ const p=vid.play(); if(p&&p.catch) p.catch(()=>{}); }catch(_){}
      const t0=performance.now();
      const anim=(now)=>{
        if(current.kind!=='presentationScrollC') return;
        const t=(now-t0)/1000, y=6*Math.sin(t*1.3);
        hand.setAttribute('transform',`translate(${400-30},${450+y*3})`);
        window.lapRaf=requestAnimationFrame(anim);
      };
      window.lapRaf=requestAnimationFrame(anim);
    }
  }

  /* ===== Écran 1 — Présentation du clic droit : format ordinateur, séquencé (clic PUIS menu) ===== */
  function cPresentation(){
    current={kind:'presentationC'}; majProgC(0);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active lap2">
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; white-space:pre-line">${CFG.presentation.ecran}</div>
      <div class="lap-wrap">${laptopSVG()}</div>
    </div>`;
    demoOrdinateur('clicDroit');
    jouerC(0, cSuite);
  }

  /* ===== Écran 2 — Phase de test : le Coffre à Bonbons (3 options, une seule correcte) ===== */
  function cPhaseTest(){
    current={kind:'phaseTestC'}; majProgC(0, true);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden; position:relative">
      ${decorScene('bonbons')}
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; margin-bottom:2vh; align-self:center; white-space:pre-line">${CFG.phaseTest.ecran}</div>
      <div style="flex:1; display:flex; align-items:center; justify-content:center">
        <img id="mcBonbons" src="${window.__resources.coffreBonbonsImg}" alt="coffre à bonbons" style="width:min(26vw,200px); cursor:pointer; filter:drop-shadow(6px 8px 0 rgba(0,0,0,.15))">
      </div>
      <div class="m1-win" id="mcPTWin"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">${CFG.phaseTest.reussi}</div></div></div>
    </div>`;
    const coffre=document.getElementById('mcBonbons'), consBox=document.getElementById('ecranBox');
    let done=false;
    coffre.addEventListener('contextmenu', e=>{
      e.preventDefault(); if(done)return;
      ouvrirMenuC(e.clientX, e.clientY, [
        {id:'ouvrir', label:'Ouvrir', emoji:'🍬'},
        {id:'fermer', label:'Fermer', img:window.__resources.iconFermerImg},
        {id:'supprimer', label:'Supprimer', img:window.__resources.iconSupprimerImg}
      ], id=>{
        if(id==='ouvrir'){
          done=true; if(typeof bipOk==='function') bipOk(); majProgC(1, true);
          const w=document.getElementById('mcPTWin'); if(w) w.classList.add('show');
          jouerVictoireC(0, cSuite);
        } else {
          if(typeof bipNon==='function') bipNon();
          consBox.textContent = id==='fermer' ? CFG.phaseTest.ratéFermer : CFG.phaseTest.ratéSupprimer;
          consBox.classList.add('cons-alt');
          cTempo(1600, ()=>{ consBox.textContent=CFG.phaseTest.ecran; consBox.classList.remove('cons-alt'); });
        }
      });
    });
    jouerC(1);
  }

  /* ===== Mission 1 — le Lapin Blanc : clic droit -> 3 options (icône + texte), version narrative ===== */
  function cMission1(){
    current={kind:'mission1C'}; majProgC(1, true);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden; position:relative">
      ${decorScene('lapin')}
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; margin-bottom:2vh; align-self:center; white-space:pre-line">${CFG.mission1.ecran}</div>
      <div style="flex:1; display:flex; align-items:center; justify-content:center">
        <img id="mcLapin" src="${window.__resources.lapinBlancImg}" alt="Lapin Blanc" style="width:min(24vw,190px); cursor:pointer; filter:drop-shadow(6px 8px 0 rgba(0,0,0,.15)); transition:transform .18s ease">
      </div>
      <div class="m1-win" id="mc1Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">${CFG.mission1.reussi}</div></div></div>
    </div>`;
    const lapin=document.getElementById('mcLapin'), consBox=document.getElementById('ecranBox');
    let done=false;
    lapin.addEventListener('mouseenter', ()=>{ if(!done) lapin.style.transform='translateY(-6px)'; });
    lapin.addEventListener('mouseleave', ()=>{ lapin.style.transform='translateY(0)'; });
    lapin.addEventListener('contextmenu', e=>{
      e.preventDefault(); if(done)return;
      ouvrirMenuC(e.clientX, e.clientY, [
        {id:'suivre', label:'Suivre le Lapin', img:window.__resources.iconSuivreImg},
        {id:'sieste', label:'Faire la sieste', img:window.__resources.iconSiesteImg},
        {id:'carotte', label:'Manger une carotte', img:window.__resources.iconCarotteImg}
      ], id=>{
        if(id==='suivre'){
          done=true; if(typeof bipOk==='function') bipOk(); majProgC(2, true);
          const w=document.getElementById('mc1Win'); if(w) w.classList.add('show');
          jouerVictoireC(1, cSuite);
        } else {
          if(typeof bipNon==='function') bipNon();
          consBox.textContent=CFG.mission1.raté; consBox.classList.add('cons-alt');
          cTempo(1600, ()=>{ consBox.textContent=CFG.mission1.ecran; consBox.classList.remove('cons-alt'); });
        }
      });
    });
    jouerC(2);
  }

  /* ===== Mission 2 — le tri des cartes-soldats : PLUSIEURS cartes en même temps, toutes à traiter ===== */
  function cMission2(){
    current={kind:'mission2C'}; majProgC(2, true);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; height:100%; min-height:0; overflow:hidden; position:relative">
      ${decorScene('cartes')}
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; margin-bottom:2vh; align-self:center; white-space:pre-line">${CFG.mission2.ecran}</div>
      <div class="mc-cartes-scene" id="mcScene" style="flex:1; gap:2.4vw"></div>
      <div class="mc-cartes-msg" id="mcMsg"></div>
      <div class="m1-win" id="mc2Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">${CFG.mission2.reussi}</div></div></div>
    </div>`;
    const scene=document.getElementById('mcScene'), msg=document.getElementById('mcMsg');
    const BESOIN=5; let restantes=BESOIN, done=false;
    for(let i=0;i<BESOIN;i++){
      const etat=Math.random()<0.5?'garde':'repos';
      const img=document.createElement('img');
      img.className='mc-carte'+(etat==='repos'?' avachie':''); img.dataset.etat=etat;
      img.src = etat==='garde' ? window.__resources.carteGardeImg : window.__resources.carteReposImg;
      img.alt='carte-soldat'; img.style.width='min(18vw,150px)';
      scene.appendChild(img);
      img.addEventListener('contextmenu', e=>{
        e.preventDefault(); if(done||img.classList.contains('sortie'))return;
        ouvrirMenuC(e.clientX, e.clientY, [ {id:'garde', label:'Garde', emoji:'🛡️'}, {id:'repos', label:'Repos', emoji:'💤'} ], choix=>{
          if(choix===img.dataset.etat){
            if(typeof bipClic==='function') bipClic();
            img.classList.add('sortie'); restantes--;
            msg.textContent = restantes===0?'Toutes traitées !' : (restantes===1?'Encore une !':'Bien vu, suivante !');
            msg.classList.add('show'); setTimeout(()=>msg.classList.remove('show'), 1000);
            if(restantes<=0){
              done=true; majProgC(3, true); if(typeof bipOk==='function') bipOk();
              const w=document.getElementById('mc2Win'); setTimeout(()=>{ if(w) w.classList.add('show'); }, 500);
              jouerVictoireC(2, cSuite);
            }
          } else { if(typeof bipNon==='function') bipNon(); }
        });
      });
    }
    jouerC(3);
  }

  /* ===== Écran 5 — Présentation du défilement à deux doigts : format ordinateur ===== */
  function cPresentationScroll(){
    current={kind:'presentationScrollC'}; majProgC(3);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active lap2">
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; top:44px; white-space:pre-line">${CFG.presentationScroll.ecran}</div>
      <div class="lap-wrap">${laptopSVG()}</div>
    </div>`;
    demoOrdinateur('deuxDoigts');
    jouerC(4, cSuite);
  }

  /* ===== Objets décoratifs qui défilent dans le couloir (décor uniquement, seul le Chat est cliquable) ===== */
  const COULOIR_DECOR=['papillonLow','lantRoseOn','lantBleuOn','itemTasse','itemGateau','itemCle'];
  function couloirDecorHTML(hauteur, evitZone){
    let html='', y=160, i=0;
    while(y<hauteur-140){
      if(!evitZone || Math.abs(y-evitZone)>220){
        const key=COULOIR_DECOR[i%COULOIR_DECOR.length];
        const x=22+((i*37)%56);
        html+=`<img class="mc-couloir-item" src="${window.__resources[key]}" alt="" style="top:${y}px; left:${x}%; width:${44+((i*13)%18)}px">`;
      }
      y+=260; i++;
    }
    return html;
  }

  /* ===== Mission 3A — couloir libre, sans chrono, réussite garantie ===== */
  function cMission3A(){
    current={kind:'mission3aC'}; majProgC(3, true);
    activerSuivi(false); cacheKompa();
    const HAUTEUR=1500;
    mainEl().innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; align-items:center; height:100%; gap:16px; padding-top:2vh">
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; white-space:pre-line">${CFG.mission3a.ecran}</div>
      <div class="mc-couloir" id="mcCouloir">
        <div class="mc-couloir-track" id="mcTrack" style="height:${HAUTEUR}px">
          ${couloirDecorHTML(HAUTEUR, HAUTEUR-260)}
          <img class="mc-chat vu" id="mcChat" src="${window.__resources.chatCheshireImg}" alt="Chat" style="top:${HAUTEUR-260}px">
        </div>
      </div>
      <div class="m1-win" id="mc3aWin"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">${CFG.mission3a.reussi}</div></div></div>
    </div>`;
    const couloir=document.getElementById('mcCouloir'), track=document.getElementById('mcTrack'), chat=document.getElementById('mcChat');
    const maxPos=Math.max(0, HAUTEUR-couloir.clientHeight);
    let pos=0, done=false;
    couloir.addEventListener('wheel', e=>{ if(done)return; e.preventDefault(); pos=Math.max(0, Math.min(maxPos, pos+e.deltaY*0.55)); track.style.transform=`translateY(${-pos}px)`; }, {passive:false});
    chat.addEventListener('click', ()=>{
      if(done)return; done=true; majProgC(4, true); if(typeof bipOk==='function') bipOk();
      const w=document.getElementById('mc3aWin'); if(w) w.classList.add('show');
      jouerVictoireC(3, cSuite);
    });
    jouerC(5);
  }

  /* ===== Mission 4 — le jardin final : pièces + buissons + coffres normaux + trésor final (pas de reset sur erreur) ===== */
  function cMission4(){
    current={kind:'mission4C'}; majProgC(4, true);
    activerSuivi(false); cacheKompa();
    mainEl().innerHTML=`<div class="screen active" style="display:flex; flex-direction:column; align-items:center; height:100%; gap:clamp(12px,2.4vh,26px); padding-top:2vh">
      <div class="ecran" id="ecranBox" style="color:#C44D59; border:3px solid #C44D59; position:relative; white-space:pre-line; max-width:min(92vw,900px)">${CFG.mission4.ecran}</div>
      <div class="mc-jardin-zone">
        <div class="mc-coin-compteur" id="mcCoinCompteur"><span>🪙</span><span id="mcCoinNb">0</span></div>
        <div class="mc-jardin" id="mcJardin"><div class="mc-jardin-track" id="mcJardinTrack"></div></div>
      </div>
      <div class="m1-win" id="mc4Win"><div class="m1-win-card"><div class="m1-win-t">Bravo, super !</div><div class="m1-win-s">${CFG.mission4.reussi}</div></div></div>
    </div>`;
    const jardin=document.getElementById('mcJardin'), track=document.getElementById('mcJardinTrack'), coinNb=document.getElementById('mcCoinNb'), consBox=document.getElementById('ecranBox');
    let pos=0, done=false, pieces=0;
    const HAUTEUR=2800, ROW=210;
    const ajouterOr=(n, x, y)=>{ pieces+=n; coinNb.textContent=String(pieces); gainVole('+'+n+' 🪙', x, y); };
    const rater=(texte)=>{
      consBox.textContent=texte; consBox.classList.add('cons-alt');
      cTempo(1600, ()=>{ consBox.textContent=CFG.mission4.ecran; consBox.classList.remove('cons-alt'); });
    };

    // Génération unique du jardin (aucune régénération en cas d'erreur : le flash rouge suffit)
    let items=[]; let y=140; let i=0; let specialAffectee=false;
    const specialIndex = 2+Math.floor(Math.random()*2); // 3e ou 4e buisson rencontré
    let compteurBuisson=0;
    while(y<HAUTEUR-160){
      const r=Math.random();
      if(i%2===1){
        compteurBuisson++;
        const isCoffre = !((compteurBuisson-1)===specialIndex) && r<0.3;
        const isSpecial = (compteurBuisson-1)===specialIndex;
        if(isSpecial) specialAffectee=true;
        items.push({type: isCoffre?'coffre':'buisson', y, special:isSpecial, id:'b'+i});
      } else {
        items.push({type:'piece', y:y+(Math.random()*60-30), id:'p'+i});
      }
      y+=ROW; i++;
    }
    if(!specialAffectee){ const bs=items.filter(it=>it.type==='buisson'); if(bs.length) bs[bs.length-1].special=true; else if(items.length) items[items.length-1]={type:'buisson', y:items[items.length-1].y, special:true, id:'bx'}; }

    const dessiner=()=>{
      track.innerHTML=''; track.style.height=HAUTEUR+'px';
      items.forEach(it=>{
        if(it.type==='piece'){
          const img=document.createElement('img'); img.className='mc-piece'; img.id=it.id;
          img.src=window.__resources.pieceOrImg; img.alt='pièce';
          img.style.left=(30+Math.random()*40)+'%'; img.style.top=it.y+'px';
          img.addEventListener('click', ()=>{ if(img.classList.contains('ramassee'))return; img.classList.add('ramassee'); if(typeof bipClic==='function') bipClic(); const r=img.getBoundingClientRect(); ajouterOr(1, r.left, r.top); });
          track.appendChild(img);
        } else if(it.type==='coffre'){
          const img=document.createElement('img'); img.className='mc-buisson'; img.id=it.id;
          img.src=window.__resources.coffreBonbonsImg; img.alt='coffre';
          img.style.left=(35+Math.random()*30)+'%'; img.style.top=it.y+'px'; img.style.width='100px'; img.style.height='100px';
          img.addEventListener('contextmenu', e=>{
            e.preventDefault(); if(done||img.classList.contains('disparu'))return;
            ouvrirMenuC(e.clientX, e.clientY, [ {id:'ouvrir', label:'Ouvrir', emoji:'🔓'}, {id:'supprimer', label:'Supprimer', img:window.__resources.iconSupprimerImg} ], choix=>{
              if(choix==='ouvrir'){ if(typeof bipOk==='function') bipOk(); img.classList.add('disparu'); const r=img.getBoundingClientRect(); ajouterOr(5, r.left, r.top); }
              else { if(typeof bipNon==='function') bipNon(); rater(CFG.mission4.ratéCoffre); }
            });
          });
          track.appendChild(img);
        } else {
          const img=document.createElement('img'); img.className='mc-buisson'; img.id=it.id; img.dataset.special=it.special?'1':'';
          img.src=window.__resources.buissonImg; img.alt='buisson';
          img.style.left=(35+Math.random()*30)+'%'; img.style.top=it.y+'px';
          img.addEventListener('contextmenu', e=>{
            e.preventDefault(); if(done||img.classList.contains('disparu'))return;
            ouvrirMenuC(e.clientX, e.clientY, [ {id:'ouvrir', label:'Ouvrir', emoji:'🔓'}, {id:'supprimer', label:'Supprimer', img:window.__resources.iconSupprimerImg} ], choix=>{
              const special = img.dataset.special==='1';
              if(!special){
                if(choix==='supprimer'){ if(typeof bipOk==='function') bipOk(); img.classList.add('disparu'); }
                else { if(typeof bipNon==='function') bipNon(); rater(CFG.mission4.ratéBuisson); }
              } else {
                if(choix==='supprimer'){
                  if(typeof bipOk==='function') bipOk();
                  const coffre=document.createElement('img'); coffre.className='mc-buisson'; coffre.style.left=img.style.left; coffre.style.top=img.style.top; coffre.style.width='130px'; coffre.style.height='130px';
                  coffre.src=window.__resources.coffreTresorFinalImg; coffre.alt='trésor';
                  coffre.addEventListener('contextmenu', ev=>{
                    ev.preventDefault(); if(done)return;
                    ouvrirMenuC(ev.clientX, ev.clientY, [ {id:'ouvrir', label:'Ouvrir', emoji:'🔓'}, {id:'supprimer', label:'Supprimer', img:window.__resources.iconSupprimerImg} ], choix2=>{
                      if(choix2==='ouvrir'){
                        done=true; if(typeof bipOk==='function') bipOk(); majProgC(5, true);
                        const r=coffre.getBoundingClientRect(); ajouterOr(100, r.left, r.top);
                        cTempo(650, ()=>allerCA(cParcours.length-1));
                      } else {
                        if(typeof bipNon==='function') bipNon();
                        const f=document.createElement('img'); f.className='mc-fumee actif'; f.src=window.__resources.fumeeImg; f.alt='';
                        f.style.left=coffre.style.left; f.style.top=coffre.style.top; track.appendChild(f);
                        setTimeout(()=>f.remove(), 1000);
                        rater(CFG.mission4.ratéCoffreFinal);
                      }
                    });
                  });
                  track.appendChild(coffre); img.remove();
                } else { if(typeof bipNon==='function') bipNon(); rater(CFG.mission4.ratéBuisson); }
              }
            });
          });
          track.appendChild(img);
        }
      });
    };
    jardin.addEventListener('wheel', e=>{
      if(done)return; e.preventDefault();
      const maxPos=Math.max(0, HAUTEUR-jardin.clientHeight);
      pos=Math.max(0, Math.min(maxPos, pos+e.deltaY*0.55));
      track.style.transform=`translateY(${-pos}px)`;
    }, {passive:false});
    dessiner();
    jouerC(6);
  }

  /* ===== Badge de fin (dernier niveau : bravo, puis bouton Suivant vers la page du Permis) ===== */
  function cBadge(){
    current={kind:'badgeC'}; majProgC(5);
    setHeaderTools(false);
    const confettis=Array.from({length:14}).map((_,k)=>{ const left=6+k*6.5, delay=(k%7)*0.32, emo=['🎉','✨','⭐','🎊','🌟'][k%5]; return `<div class="conf" style="left:${left}%;animation-delay:${delay}s">${emo}</div>`; }).join('');
    mainEl().innerHTML=`<div class="screen active">
      <div class="victoire">
        ${confettis}
        <div class="medaille-img" style="font-size:clamp(70px,12vw,130px)">🏅</div>
        <h1 id="titre">Bravo !</h1>
        <div class="kompa-bulle sans-queue" style="position:static; white-space:pre-line; max-width:min(640px,64vw)">${CFG.badge.ecran}</div>
        <button class="btn" id="mcSuivantBtn" style="opacity:0; transform:translateY(14px); transition:opacity .6s ease, transform .6s ease">Suivant ▶</button>
      </div>
    </div>`;
    bipOk(); try{ pqSetFait('C'); }catch(_){}
    try{ if(typeof pfMarquerFin==='function') pfMarquerFin(); }catch(_){}
    cTempo(1800, ()=>{
      const b=document.getElementById('mcSuivantBtn');
      if(b){
        b.style.opacity='1'; b.style.transform='translateY(0)'; if(typeof bipOk==='function') bipOk();
        b.addEventListener('click', ()=>{ bipClic(); if(typeof pfOuvrir==='function') pfOuvrir(); });
      }
    });
    jouerC(7);
  }

  const cParcours=[ cPresentation, cPhaseTest, cMission1, cMission2, cPresentationScroll, cMission3A, cMission4, cBadge ];
  function majRetourC(){ if(typeof retourBtn!=='undefined' && retourBtn){ retourBtn.disabled=(cEtape===0); } }
  function majNavC(){
    const nav=document.getElementById('nbNav'); if(!nav) return;
    const prec=document.getElementById('nbPrecBtn'), suiv=document.getElementById('nbSuivBtn');
    if(prec) prec.disabled=(cEtape===0);
    if(suiv) suiv.style.display=(cEtape===cParcours.length-1)?'none':'';
  }
  function allerCA(i){
    cEtape=Math.max(0,Math.min(cParcours.length-1,i)); clearC();
    try{ if(window.speechSynthesis) speechSynthesis.cancel(); }catch(_){}
    cParcours[cEtape](); majRetourC(); majNavC();
  }

  function lancer(){
    debloqueAudio(); musiqueStart(); musiqueDuck(true);
    if(typeof pqEl!=='undefined' && pqEl) pqEl.classList.remove('on');
    setHeaderTools(true);
    if(typeof retourBtn!=='undefined' && retourBtn) retourBtn.onclick=()=>allerCA(cEtape-1);
    const nav=document.getElementById('nbNav'), prec=document.getElementById('nbPrecBtn'), suiv=document.getElementById('nbSuivBtn');
    if(nav){ nav.style.display=cAudioPret()?'none':'flex'; if(prec) prec.onclick=()=>{ bipClic(); allerCA(cEtape-1); }; if(suiv) suiv.onclick=()=>{ bipClic(); allerCA(cEtape+1); }; }
    bipClic(); cEtape=0; allerCA(0);
  }
  function stop(){
    clearC();
    try{ if(window.speechSynthesis) speechSynthesis.cancel(); }catch(_){}
    if(typeof retourBtn!=='undefined' && retourBtn) retourBtn.onclick=()=>allerA(etape-1);
    const nav=document.getElementById('nbNav'); if(nav) nav.style.display='none';
    const n4=document.querySelector('#steps .snode[data-i="4"]'); if(n4) n4.style.display='';
  }
  return { lancer:lancer, stop:stop };
})();
window.NC = NC;
function lancerNiveauC(){ if(window.NC) window.NC.lancer(); }
/* =================== FIN NIVEAU C =================== */


majProg(); majRetour(); ouvrirParcours(); document.addEventListener('pointerdown', ()=>debloqueAudio(), {once:true});